[MERCURY-BRIDGE] Index contains 16420 chunks
[MERCURY-BRIDGE] Query router: type=landmine mode=hybrid-classified boost=landmine top-k=8
[MERCURY-BRIDGE] Rationale: landmine/rules pattern detected; boost landmine and guardrails
[MERCURY-BRIDGE] Embedding query for starter context...
[MERCURY-BRIDGE] Starter context: 8 chunks
  1. ogz-meta/ledger/frontend/js/panels/watchlist-strip.js:635-678 (method: init) sim=0.586
  2. ogz-meta/cognition-history/mercury/dashboard-chart-asset-routing-recheck-2026-05-29.md:1-29 (doc_section: root) sim=0.661
  3. ogz-meta/cognition-history/mercury/dashboard-chart-asset-routing-2026-05-29.md:1-25 (doc_section: root) sim=0.636
  4. ogz-meta/cognition-history/mercury/dashboard-three-file-drop-2026-05-26.md:1-29 (doc_section: root) sim=0.661
  5. ogz-meta/cognition-history/mercury/session-router-broker-rest-reconciliation-2026-05-29.md:1-29 (doc_section: root) sim=0.605
  6. ogz-meta/cognition-history/mercury/candle-ingress-scope-stamping-2026-05-26.md:1-28 (doc_section: root) sim=0.619
  7. ogz-meta/cognition-history/mercury/dashboard-final-live-report-expanded-2026-05-26.md:1-35 (doc_section: root) sim=0.617
  8. ogz-meta/cognition-history/mercury/ttp-consistency-profit-cap-recheck-2026-05-24.md:1-23 (doc_section: root) sim=0.606
[TRACE-MEMORY] Eviction: 0 stale + 0 over-cap, 467 remaining
[MERCURY-BRIDGE] Prior trace hint found (similarity: 0.91, 13 iters, used 0x)
[MERCURY-BRIDGE] Tool adapter ready. Tools: grep, open_file, get_chunk, list_files, tavily_search, git_show, web_fetch
[TRAI] LLM client initializing...
   Provider: Mercury-2 (Inception Labs)
   Model:    mercury-2
   Endpoint: https://api.inceptionlabs.ai/v1
[TRAI] LLM warm-up complete (266ms)
[TRAI] LLM client ready. Provider: Mercury-2 (Inception Labs) | Model: mercury-2
[MERCURY-BRIDGE] Starting ReAct loop (max 60 iterations)...
[MERCURY-BRIDGE] Trace inserted (quality=164.6)

═══ ANSWER ═══

**1. Repeated `init()` → `teardown()` → `init()` misses handlers on the same Socket**
*State socket‑handler tracking* is stored in `state.socketHandlerSocket` (lines 98‑99).
During `init()` the handlers are attached **only when** the current socket differs from the stored one (lines 631‑635).
`teardown()` never clears `state.socketHandlerSocket` (the block 766‑768 does not touch it).

Consequences:

```text
// 1️⃣ first init() → socket stored, handlers registered
// 2️⃣ teardown() → DOM cleared, interval cleared, but state.socketHandlerSocket still points to the old socket
// 3️⃣ second init() with the same socket → state.socketHandlerSocket === socket → condition false → NO registerHandler() call
```

Thus the watch‑list stops receiving `price`/`ticker_price` events after a teardown‑re‑init cycle.

*Evidence* – `state.socketHandlerSocket` definition [watchlist‑strip.js:98‑99] and the guard in `init()` [watchlist‑strip.js:631‑635]; `teardown()` never resets it [watchlist‑strip.js:766‑768].

---

**2. Position‑sync interval can leak or fail to restart**

`init()` creates the interval only when `state.positionSyncIntervalId` is falsy (lines 641‑643).
`teardown()` clears the interval and sets the ID to `null` (lines 756‑758).

If a component is removed **without** calling `teardown()` (e.g., a UI change that discards the strip but leaves the JS object alive), the interval remains active forever, continuing to call `syncPositionStates()` and holding a reference to the old ticker list. This is a classic leak.

*Evidence* – interval creation [watchlist‑strip.js:641‑643] and clearance [watchlist‑strip.js:756‑758].

---

**3. Concrete `ticker_price` frame that the handler silently drops**

`onPriceEvent` discards any frame whose price is not a positive finite number (line 544).
A frame generated by `server/dashboard-ticker-frame.js` with a zero or negative price therefore never reaches the UI, even though the frame is otherwise well‑formed.

Example frame (produced by `buildTickerPriceFrame` with `price: 0`):

```js
{
  type: 'ticker_price',
  symbol: 'BTC',
  price: 0,          // <-- zero price
  timestamp: 1700000000
}
```

The server code that builds the frame is at lines 61‑65 (`positiveNumber` conversion) and the early‑exit check is at line 544 in the client.

*Evidence* – price extraction & validation in `onPriceEvent` [watchlist‑strip.js:538‑544]; `buildTickerPriceFrame` positive‑number guard [dashboard‑ticker‑frame.js:64‑65].

---

**4. Default‑watchlist update of the wrong broker card**

`findTickerForPriceSymbol` (lines 123‑135) resolves a symbol to a ticker **only if** the broker hint matches; otherwise, when more than one ticker shares the same normalized symbol, it returns `null`.

If a custom watch‑list contains two entries for the same symbol with different brokers (e.g., `{symbol:'BTC',broker:'KRA'}` and `{symbol:'BTC',broker:'CB'}`) and a `ticker_price` frame arrives **without** a broker hint, the function returns `null` (line 134) and the price update is silently ignored, leaving the wrong broker card unchanged.

*Evidence* – ticker‑resolution logic [watchlist‑strip.js:123‑135] and the early‑return when ambiguous [watchlist‑strip.js:134].

---

**5. Test blind‑spot that still passes the new tests**

`watchlist-symbol-match.test.js` checks that `registerHandler` is called **exactly twice** after the sequence `init(); init(); teardown(); init();` (lines 108‑110).
Because the production code never re‑registers after a teardown (see point 1), the test **does not verify** that the handlers are present after the final `init()`. It only asserts the *total* call count, not that the handlers are attached to the *current* socket.

Similarly, the test asserts that the position‑sync interval is created (`setIntervalMock` called twice) and cleared (`clearIntervalMock` called with the first interval ID), but it does **not** confirm that a fresh interval is re‑started after teardown.

Thus the test suite gives a false sense of confidence while the real‑world bug (missing handlers after a teardown‑re‑init) remains unchecked.

*Evidence* – test expectations [watchlist‑symbol‑match.test.js:108‑115] and the missing verification of handler re‑attachment after `teardown()`.

---

### Summary of concrete failure paths

| Objective | Concrete sequence / frame | Location in code |
|-----------|---------------------------|------------------|
| 1. Duplicate/missing handlers after teardown | `init()` → `teardown()` → `init()` (same socket) → no `registerHandler` call | state guard [watchlist‑strip.js:631‑635]; no reset [watchlist‑strip.js:766‑768] |
| 2. Position‑sync interval leak | Remove strip without calling `teardown()` → interval stays alive | interval creation [watchlist‑strip.js:641‑643] |
| 3. Ignored `ticker_price` frame | `{type:'ticker_price',symbol:'BTC',price:0,timestamp:…}` | price check [watchlist‑strip.js:544]; frame builder [dashboard‑ticker‑frame.js:64‑65] |
| 4. Wrong broker update (ambiguous symbol) | Watch‑list contains two `BTC` entries, frame without broker hint → update ignored | ticker resolution [watchlist‑strip.js:123‑135] |
| 5. Test blind‑spot | Test only counts total `registerHandler` calls, never asserts re‑registration after teardown | test expectations [watchlist‑symbol‑match.test.js:108‑115] |

These findings expose the exact code locations that cause the contract violations and demonstrate why the current test suite does not catch them.

[iterations: 14 | termination: answer_given | latency: 24621ms]
