# INHERITED — MISSION 0: STOP 1 LEAF MANIFEST

Pre-existing conditions found during enumeration that are NOT this mission's to fix. Recorded so they stop being rediscovered. Items marked ✔ were re-verified on HEAD at packet close; unmarked items are carried from enumeration agents' reports and should be re-verified before action.

1. **✔ DynamicTrailingStop.js is a corpse.** Zero live requires; logic lives in ExitContractManager. Its config leaves (exitContracts.*.trailingStopPercent/trailingActivation on percent-family contracts) have only this dead module as their trigger-side reader.
2. **✔ MaxProfitManager.js does not exist** in core/ or modules/, yet `exitLogic.maxProfitManager.*` config leaves (MPM_TRACK_PERFORMANCE, MPM_LOG_LEVEL, MPM volatility keys) survive in ConfigLoader (:2854–2857, :2877–2882). Config for a component that was dissolved.
3. **✔ TRAIDecisionModule config self-clobber** (TRAIDecisionModule.js:64–65): `...config` spread is immediately followed by `enableVetoPower: false` — the TRAI_VETO env/config value can never enable veto power through this constructor.
4. **✔ TakeProfitChecker is constructed but never fires.** Instantiated (ECM.js:262) yet `.check()` has no live call site; takeProfitPercent's only live effect is order-sizing caps (OrderExecutor.js:2222–2223). TP-as-trigger is dead across all percent-family contracts.
5. **✔ Nine dead invalidation labels.** JSON `invalidationConditions` strings with no ECM dispatcher case: ema_pullback_invalidated, ema_retest_failed, liquidity_absorbed, sr_break, pattern_invalidated, sweep_absorbed, break_retest_invalidated, fvg_filled, or_break_reversal. Configured intent that cannot execute.
6. **✔ Three unreachable exit contracts.** exitContracts.BreakRetest, .CandlePattern, .MarketRegime have no corresponding strategies.* block — 43 config leaves (X0032–X0049, X0081–X0108) that no trade can ever bind to. Related: pipeline.enableCandlePattern / enableBreakRetest / enableMarketRegime toggles in every launch profile gate strategies that don't exist as config blocks (MarketRegimeDetector is an orphan module).
7. **✔ The JSON base layer is largely decorative.** ConfigLoader BASE_CONFIG builds most sections from env()/literals and never reads the JSON copy (rows annotated `INERT-shadowed`); values match by coincidence of duplication. Exceptions where JSON IS live: the five `requiredConfiguredPlainObject` exit contracts, strategies.* (`requiredConfig*` accessors), launchProfiles, tuningProfiles definitions, and scattered `requiredConfigured*` leaves — the manifest's source_layer column is the per-leaf map.
8. **✔ backtest-ttp-5k-max misnomer:** profile named for TTP evaluation but ships `venueGuards.ttp.enabled=false` (trading.config.json:707).
9. **✔ scaleIn.addSizingLadder has no consumer.** Cloned defensively (ECM.js:230–232), read nowhere else — a config surface waiting for a feature, or dead.
10. **Codex Aug 16 literal census has drifted** (enumeration agents reported the literal population moved since the census was cut). The census remains the denominator per dispatch; Z rows record the reconciliation. Re-count belongs to a future pass.
11. **exitContracts._validated stamps are metadata theater:** carried into frozen policy as `validatedAt` (PolicyBuilder.js:271) and referenced by a deleted DTO; most are null or a fixed 2026-03-20. No behavior reads them.
