# Work

Operator correction: the addition landed in `10a5ed3e`, but no indexer or provider run was started with it. Removing that single target restores the prior selection policy. Mercury-specific instructions must be explicit rather than inferred from builder alignment. The unrelated evidence-delivery, verdict, read-range and candidate-refiling repairs are not reverted by this correction.

One production-tool line added in indexer.js: `ogz-meta/Alignment/TREY-DOCTRINE-FABLE-LANE.md`. No ignore relaxation, new ingestion framework, bot behavior or database write. Packet Git history identifies the atomic commit. Commit/push and later indexing are distinct operations.
