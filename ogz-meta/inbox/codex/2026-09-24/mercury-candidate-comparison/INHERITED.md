# Inherited

The old whole-file/inheritance/classifier policy remains unchanged in doctrine-review.js. Current direct instructions distinguish builder doctrine from Mercury rules. This prompt change neither resolves that policy conflict nor deletes missing-evidence flags to produce a pass.

The previous run's incorrect config-absence allegation and recheck scope shrink motivated explicit candidate comparison and base/override distinction. Current dirty Stop 1 source is rejected work, not accepted implementation; it is not edited or approved by this mission. Registry completion remains unperformed.
