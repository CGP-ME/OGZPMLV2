# Mercury candidate collection and comparison

Trey's current instruction: "it does not take the first answer it finds it needs to build an exhaustive list evaluate all possible answers then pick the one or ones that best fit and why". The purpose is reliable blast-radius investigation before the final Stop 1 configuration migration and validation. Existing authorization: one logical change per commit, commit and push paired. No bot activation or premature registry completion.

Scope: strengthen the existing agentic.systemPrompt in mercury.config.json. No new gate, flag, setting, module, helper script or workflow.
