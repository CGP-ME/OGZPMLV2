# Seeded Mercury review of a hypothetical configuration migration

Trey: "lets test it out" and "give it something difficult maybe even bake an error in and see if it catches it or it performs as it should". Date 2026-09-24. Continue the actual Mercury/Fable/recheck/Kimi chain at f9f966470c5f81b64459d50f6ef4e707c5de0b08. One review at a time, 7,750 tokens, no arbitrary iteration ceiling. No bot restart, configuration mutation or broker action.

The only planted error is a standalone, unapplied patch under fixtures/. Current production source remains unchanged. The model receives that patch as explicit host-attested evidence plus the actual repository read-only tools. The expected behavioral comparison is recorded separately before dispatch and excluded from Mercury's prompt and retrieval. This is a targeted, user-authorized fault-injection task, not a secretly narrowed broad audit or final Stop 1 acceptance.

Storage: new empty database ogz_mercury_recovery_20260924_007, existing configuration mechanism MERCURY_CONFIG_FILE. Existing ignore policy, model identities, tools and authority ceilings stay unchanged. No new framework or helper script.
