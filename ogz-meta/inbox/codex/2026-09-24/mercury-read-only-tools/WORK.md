# Work

Inspected actual public registry, native schema, text tool documentation, execute dispatch, run_check command validation and subprocess path. The inherited interface advertised and dispatched executable commands; a tracked-file copy was not an OS boundary for ordinary repository programs.

Removed run_check from all three advertised/callable surfaces. Replaced instructions to execute commands with explicit read-only review and host-receipt attribution. Kept historical receipt readers/assessors and private legacy implementation, which is not exported or registered. This is capability removal, not a new gate or sandbox.

Observed actual adapter before and after via observe-interface.cjs; syntax and scoped diff checks completed. No provider invocation, Jest, bot execution or PM2 operation for this repair.
