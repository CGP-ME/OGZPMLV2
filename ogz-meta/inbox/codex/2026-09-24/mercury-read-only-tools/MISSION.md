# Read-only Mercury tools

Authority: Trey's current clarification that Mercury does not write and Mercury-specific rules should be explicit. Existing authorization to land finished Mercury fixes atomically. Scope: tool-adapter public interface and its two command-execution prompt directives; no bot source, execution semantics, scope narrowing or evidence-ceiling changes.
