# Direct captured-response evidence

Original source SHA-256 c39db4e29056904818fee749ac4bc601b68fe7b9c54060210405bf284365304f; corrected source SHA-256 5d789d634d45ecfa33a2bdb2c4929be7f9215afae9b9333792c0aa10f16b7225. OBSERVATIONS.json records before/after outcomes and the actual-loop replay. Both use the preserved raw response identified in MANIFEST.md. No new provider call, broker action, bot boot or index operation.

Transformer replay (run before and after the edit; prior source is retained in parent Git):

```bash
node <<'NODE'
const fs=require('fs'),crypto=require('crypto');
require('./trai_brain/mercury-bridge/indexer');
const {normalizeToolHandleCitations}=require('./trai_brain/mercury-bridge/react-loop');
const p='ogz-meta/cognition-history/mercury-runs/raw/2026-09-24/2026-09-24t04-29-08-487z-543152-d36a6dc91699/mercury-20.raw';
const bytes=fs.readFileSync(p),original=JSON.parse(bytes).choices[0].message.content;
const rendered=normalizeToolHandleCitations(original);
console.log(JSON.stringify({path:p,rawSha256:crypto.createHash('sha256').update(bytes).digest('hex'),sourceSha256:crypto.createHash('sha256').update(fs.readFileSync('trai_brain/mercury-bridge/react-loop.js')).digest('hex'),originalHasFalseConfigCitation:original.includes('foundation/ConfigLoader.js:160-191'),renderedHasFalseConfigCitation:rendered.includes('foundation/ConfigLoader.js:160-191'),originalExcerpt:original.split('\n').find(l=>l.startsWith('The examined changes')),renderedExcerpt:rendered.split('\n').find(l=>l.startsWith('The examined changes')),rawAnswerPreserved:rendered===original,providerCalls:0},null,2));
NODE
```

Actual-loop replay with all transport synthetic, not another provider investigation:

```bash
node <<'NODE'
const fs=require('fs');require('./trai_brain/mercury-bridge/indexer');const {runReactLoop,normalizeToolHandleCitations}=require('./trai_brain/mercury-bridge/react-loop');
const p='ogz-meta/cognition-history/mercury-runs/raw/2026-09-24/2026-09-24t04-29-08-487z-543152-d36a6dc91699/mercury-20.raw';const answer=JSON.parse(fs.readFileSync(p)).choices[0].message.content;
(async()=>{let calls=0;const result=await runReactLoop({client:{generateWithTools:async()=>({content:++calls===1?'CANDIDATE SET\nSynthetic transport replay; no new investigation. foundation/ConfigLoader.js:1846-1860':answer})},toolAdapter:{buildToolSchema:()=>[],execute:async()=>{throw Error('Unexpected tool call in replay');}},userQuery:'Mercury, break my fix.'});console.log(JSON.stringify({kind:'actual loop with captured response replay and synthetic candidate; not a new provider review',calls,termination:result.termination,answerPreserved:result.answer===answer,quality:result.answerQuality,knownLiteralFormats:[normalizeToolHandleCitations('File: `trai_brain/mercury-bridge/tool-adapter.py` Lines: 1073-1082'),normalizeToolHandleCitations('`mercury.config.json` lines 68-69 show the new wording.')],providerCalls:0},null,2));})().catch(e=>{console.error(e.message);process.exitCode=1;});
NODE
```

WHAT I DID: reproduced a false citation generated by host code from a real response and removed its producer.
WHAT I DID NOT DO: prove every model citation, exhaustive investigation or Stop 1 acceptance; run Jest or change the recovered untracked test file.
WHAT I ASSUMED: the original response is replay input, never a correct-answer oracle.
