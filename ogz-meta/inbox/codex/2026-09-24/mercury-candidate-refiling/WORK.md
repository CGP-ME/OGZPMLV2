# Work

Parent: `5ab460db` (full parent/result SHAs in Git). This packet's commit changes react-loop.js collection state/prompt and run-ledger.js revision preservation, plus this packet and its changelog entry. Commit and push are executed separately, not presumed here.

A candidate response after an existing candidate previously became the final answer. Additional tools also allowed a subsequent decision against the old inventory. Revised inventories now stay in collection; tools after filing require a revised inventory before decision. Prior inventories survive in candidate.revisions and the ledger. No new count-based completion claim, arbitrary ceiling, provider route or bot gate. The broader recovered prototype's changed-file coverage controller was not transplanted as exhaustive dependency proof.
