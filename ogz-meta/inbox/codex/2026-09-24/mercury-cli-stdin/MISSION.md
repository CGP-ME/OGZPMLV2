# Mercury CLI evidence transport

One logical Mercury-only repair under Trey's recovery/landing authorization: deliver full reviewer prompts through the existing Claude CLI without exceeding the OS argument limit. No evidence truncation, new provider fallback, bot change or runtime activation.
