# Inherited limits

- Static import caller discovery does not close JSON, computed-key, runtime or dataflow dependencies.
- Reference discovery can truncate. Ten of sixteen actual reference scans here remain truncated; the writer preserves that fact.
- The current prompt still contains the inline scan; this repair does not implement new compaction or prove the model read every section.
- Existing scanner inventory and downstream candidate/authority defects require their own verified repairs. No authority ceiling is relaxed here.
- Existing provider failure/absence receipts remain unaccepted. Candidate isolated index is not bound to this newly changed source and has unresolved doctrine-selection scope.
- Filesystem write failures can leave a partial private artifact. Existing failed-dispatch receipt retains the original failure; this change does not invent a successful bundle pointer.
- Existing dirty Stop 1 implementation remains separate and unapproved. None of it is staged by this mission.
