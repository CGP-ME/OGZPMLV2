# Work

Parent: `9aa33f8e78150b05516d0d157aaf13752f953bc2` on `astra-era`. The commit containing this packet is the atomic evidence-handoff repair; resolve its SHA through this packet's Git history. Commit/push execution follows reviewed staging, not presumed here.

Changed production-tool paths: `trai_brain/mercury-bridge/ask.js`, `trai_brain/mercury-bridge/run-ledger.js`. Changelog entry and this packet accompany that one behavior.

Recovered the existing writer from immutable `OGZPMLV2_RECOVERY_20260921T195912Z`, then connected structured scan sections, per-run write, model-visible artifact location, ledger pointer and terminal receipt. Preserved its private modes and exclusive writes. Reference JSON is pretty-printed only in the stored artifact so line-range reads can divide large results. Existing inline prompt remains unchanged apart from the artifact pointer.

Did not restore unrelated candidate changes or its Jest file. Did not introduce recovered internal-argument guard throws: the existing producer supplies the root, generated run ID, timestamp and typed section content, and the caller invokes the writer only for nonempty sections. Existing filesystem failures reach the existing failed-dispatch receipt.

Next: reconcile recovered Mercury review consumption/finalization and isolated corpus scope, then perform the authorized complete provider chain. This local handoff check is not that chain or Stop 1 acceptance.
