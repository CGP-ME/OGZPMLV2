# Manifest

- Source: direct read-only audit of `e54a8b8` runtime/config sources plus packet commit `079f91ab`, performed 2026-09-07.
- Kept: `DIRECT-RUNTIME-CONFIG-AUDIT.md` records file:line call-chain receipts for the Stop-1 config reachability question, the trailing-contract rows, the manifest count mismatch, the Sept. 7 fifteen-key surface, and the missing cooldown/eight-stamp evidence completed for 0.1a.
- Kept: `DELETE-ROW-RECEIPT.tsv` accounts by id for all 79 rows that entered 0.1a as `DELETE-AS-DEAD` and records the audited result and corrected disposition.
- Why kept: it corrects false no-reader claims in `MANIFEST.tsv` and distinguishes live JSON inputs from hardcoded shadows without relying on Mercury or another model.
