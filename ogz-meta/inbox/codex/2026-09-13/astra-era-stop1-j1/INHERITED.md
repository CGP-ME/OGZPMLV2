# STOP 1 J1 inherited and deferred boundaries

## Final configuration ownership joins J1 and J7/J8

The ruled target remains:

- customer/behavior settings: `config/settings.json`;
- static implementation values: `config/internals.json`;
- credentials and true bootstrap inputs: the single bootstrap source;
- one accepted ConfigLoader revision and one durable settings writer.

Trey rejected a temporary `process.env` projection on 2026-09-13. J1 therefore cannot remove credential producers independently of the final J7/J8 cut. The combined change creates the three storage classes above and one immutable ConfigLoader runtime owner. A source match in the 41-file census remains an investigation lead until its live caller and destination are proved.

## Remains for J2

- Exclusive singleton acquisition and its direct runtime receipt.
- Singleton policy itself, including skip semantics.

The atomic configuration cut moves SingletonLock's existing inputs to the accepted runtime snapshot; J2 still owns exclusivity and lock policy.

## Remains for J3

- One operator shutdown and failure lifecycle.
- Removal of competing uncaught/rejection/signal owners.
- Whether and when a startup failure changes process exit status.
- Waiting for attempted remote reporting before termination.

J1 records the current outcome without adding or preserving process-death authority.

## Remains for J4

- Required versus optional component classification.
- Route-specific broker credential requirements.
- Removal of the unconditional Kraken credential check on Alpaca-only routes.
- Retry and recovery ownership.
- Trading readiness when configuration or a required service is unavailable.

J1 supplies truthful absence/source facts; it does not decide readiness.

## Included in the atomic J1 + J7/J8 cut

- Keys-only versus behavioral environment migration.
- One accepted configuration revision and nested fingerprint repair.
- Removal of mode, tier, strategy, sizing, exit, and other behavioral environment owners.
- PM2 descriptor cleanup beyond credential-source ownership.

There is no interim ambient projection. The destination owner and every live consumer move together before duplicate dotenv readers and ambient reads are removed.

## Remains for J15

- Full notification event selection.
- Retry policy and recovery.
- Attempt versus service acceptance versus watched-phone delivery.
- Publisher-off write-path work.

The atomic cut may name `unavailable`, `scheduled`, `invoked`, or the observed request result; it cannot claim service acceptance or phone delivery without that evidence.

## Split between the atomic cut and J17 tooling behavior

- Mercury bridge/direct-question provider credential loading, transport, cost, model, and receipt behavior.
- Gate, downloader, and one-off script credential boundaries.

The bot's local read-only toolbox must stop importing provider/embedding initialization merely to obtain repository ignore policy. The atomic cut extracts that policy and reports optional toolbox acquisition through its real owner chain. Provider behavior remains J17.

## Preserved supervisor-owned boundaries

- `SUPERVISOR_ALERT_HOOK` remains a conditional module-extension path. The atomic configuration cut delivers its accepted path explicitly without enabling, removing, or redesigning the hook.
- `supervisor-hmac.key` remains generated, persisted, and consumed by `core/Supervisor.js`. It is not a bootstrap input and does not move into source receipts.

## Remains for STOP 2 and later packages

- Candle acquisition/admission and removal of acquisition-derived trading authority.
- SessionRouter startup restoration, switching, and complete symbol coverage.
- Execution mode propagation, exit policy, sizing, fees, cooldown, halts, and daily loss.

## Security incident during this investigation

A read-only source-census command accidentally matched credential-named assignments in the ignored local `.env` and displayed their values in the private Codex tool transcript. No value is reproduced, transformed, hashed, or committed in this packet. The repository was not modified by the command, and there is no evidence that the values were committed, pushed, published, or sent through an external review seat. This packet does not impose blanket rotation. The operator decides whether the Codex transcript lies outside the trusted boundary; any active credential treated as disclosed under that policy must be rotated before runtime acceptance.

The ignored file is covered by `.gitignore:10`. It was not read during this correction.

During the independent Astra check, a display-redaction pattern also missed the already tracked hardcoded Sentry DSN assignment in `instrument.js`, so that tracked value appeared again in the private tool transcript. It is not repeated here. This did not read a private source or expand the repository's existing tracked exposure; removing that fallback remains part of the atomic cut.

## Footer

WHAT I DID: joined the credential and final configuration ownership cuts while preserving later lifecycle, readiness, notification, and provider boundaries; disclosed the earlier transcript incident without reproducing values.

WHAT I DID NOT DO: rotate credentials, edit `.env`, access provider consoles, or treat the ignored file as safe merely because it is untracked.

WHAT I ASSUMED: Trey determines the transcript trust boundary and any resulting rotation before real authentication or PM2 acceptance.
