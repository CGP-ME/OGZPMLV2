# Mission

Continue authorized Stop 1 migration, one connected change/commit/push. Connect the existing PropSafeEMAPullback entry and max-hold/initial-stop settings to retained per-symbol consumers through the existing settings API, correcting bypass of its configured ATR period. No confidence formula, RTH schedule, registration, execution mode, new global stop, provider/index, deployment or broker change. Prior floating implementations are not authority.
