# Evidence

Current evidence supersedes the old auth and pending-review absences below: full four-file review plus source/actual-receipt reconciliation completed. Focused resolution run 2026-09-26T12-17-09-218Z-c648f2f0ce5f returned pass, all required seats completed, source unchanged. Exact raw answers, tools, usage, prior disagreements and attested source are in review-three/review-four tapes. Actual StateManager save/load and ECM equality are in state tapes. Cold pull and deployed behavior are still separate, not inferred from review.

Observation precision: the recorded-candle fixture calls createExitContract with a third argument, which that method ignores. The receipt's returned canonical TSM contract, not those unused proposed overrides, is the actual input. The policy-freeze comparison and independent entry-plan observations use that returned contract. No claim that the unused override stop/lookback reached production is made.

Counted local scope: 14/14 new managed-stop controls observed separately; 798 long/short recorded-data states compared before/after; 4 stock/crypto long/short entry plans before and 4 after; 2 zero-floor boundary fixtures. This is not a whole-repository or live-runtime denominator.

Commands executed from /opt/ogzprime/OGZPMLV2:

```
node ogz-meta/inbox/codex/2026-09-26/stop1-entry-exit-settings/fixtures/blast.cjs
node ogz-meta/inbox/codex/2026-09-26/stop1-entry-exit-settings/fixtures/observe.cjs baseline
node ogz-meta/inbox/codex/2026-09-26/stop1-entry-exit-settings/fixtures/observe.cjs candidate
node ogz-meta/inbox/codex/2026-09-26/stop1-config-connections/fixtures/review.cjs entry-exit-settings-preflight --preflight
node ogz-meta/inbox/codex/2026-09-26/stop1-entry-exit-settings/fixtures/checkpoint.cjs
```

Existing Mercury/Serena substrate: 389 enumerated files, 388 parsed, one named ogz-run.js return-outside-function parse failure, 34 selected references and 7 candidate calls. Direct dependency callers: PolicyBuilder 2, ECM 4, TradingLoop 1, ConfigLoader 62. These static candidates do not prove executable completeness. Raw census/file hashes: tapes/blast.json.gz.

Recorded input: tuning/tsla-15m-tiny.json, 500 bars; exact hash in OBSERVATION.json. Baseline: old policy observations change 389/798 after the actual loader reload; settings-save unsupported. Final candidate: old observations change 0/798, all saved fields reach the frozen policy, 14 independently varied controls affect their actual new-trade consumer, false/zero retained, five invalid requests preserve accepted configuration, cold module reload matches the saved identity. Initial 375 trailing updates / 363 break-even updates are bounded fixture outcomes, not fills. Byte-identical old policy and JSON round-trip/hash preserved.

Real OrderExecutor._buildEntryPlan and real PolicyBuilder are exercised with inert unrelated service imports. Actual StateManager methods/persistence were SOURCE-TRACED, not executed. The fixture records stock-share-range disabled to isolate this policy producer; it does not certify sizing. Actual receiver/AtomicWrite operate on disposable files. Full ECM observations use explicit synthetic trade identity/quantity/lifecycle fields on recorded prices. One-field arithmetic comparisons and zero-floor checks use explicitly synthetic boundary values. No simulated boot is passed off as running-engine proof.

Provider preflight: Mercury ready; Kimi ready; Fable live result: `401 OAuth access token has expired`. Fable applied-model observation is claude-fable-5, but failed authentication supplies NO code review. Raw responses, invocation, completion and ledger row are retained in the packet, redacted with original/redacted/gzip hashes. The 14 exported files total 844,488 compressed bytes, zero known credential matches, all compressed and uncompressed hashes reread successfully. Original artifacts remain on-box.

Failed fixture observations are preserved: initial receipt serialization called the unexported loader.getReceipt (fixture defect, corrected to the real getSettingsView interface); zero-floor assertion exposed the actual consumer check before clamping (source corrected). Neither failed observation is reported as a pass.

Not proved: full adversarial acceptance, deployment, real browser behavior, StateManager persistence/restoration, actual phone notification delivery, cold-pull reproduction of this pending source, complete UI inventory or whole Stop 1. Existing TraceSpine subscriber/notification selection was exercised, not transmitted.

Continuation supersedes only the named readiness and local state-round-trip absences above: fresh preflight succeeded for all three providers; `node fixtures/state-roundtrip.cjs` (packet-relative) exercised actual StateManager save/load on disposable state with four synthetic records. Receipt private/state-roundtrip-GGskPb/receipt.json records exact source/input hashes, state hashes, settings-save response and before/after ECM outputs. Values/hash survived; load does not Object.freeze restored policy objects. No deployed state was read or modified. All other deployment/browser/whole-Stop-1 limits remain.

Additional tapes: review-one-MANIFEST.json preserves fresh preflight, the attestation refusal and first complete disagree run; review-two-MANIFEST.json preserves the failed supplemental-delivery follow-up. Both exports reread compressed/uncompressed hashes and found zero known credential matches. Prior referenced run rows can also appear in these exports because the original exporter selected run IDs cited in console output; these are historical context, not additional executions.
