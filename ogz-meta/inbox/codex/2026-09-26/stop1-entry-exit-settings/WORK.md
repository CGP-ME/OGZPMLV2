# Work

Main/index checked: astra-era e3fa99f2, empty index, inherited dirt preserved. Existing verification clone fast-forwarded from origin to e3fa99f2, tracked clean. Read PolicyBuilder, FrozenExitPolicy, ECM, actual OrderExecutor/StateManager producers, both TradingLoop consumers, BreakEvenManager, TraceSpine and NtfyTraceNotifier. R4 historical handoff is a navigation lead; current entry-preservation instruction controls.

Current defect: frozen policy omits trail; ECM constructor captures trail once, break-even and fee buffer read current globals. Existing partial/tier plan and legacy 1R break-even checker are separate owners, not silently changed here.

Candidate authored only in the existing verification clone (exact location in OBSERVATION.json): PolicyBuilder freezes the existing complete trail object; ECM uses entry-owned trail, break-even and fee buffer; both TradingLoop hold receipts retain the update results; ConfigLoader's existing save/read surface connects fourteen actual managed-stop fields. No new file/owner/framework in production. No new throw, process stop, entry halt, broker call, or startup refusal. Three legacy-policy absence branches exclude only the unprovable update, retain known stops, and emit an existing TraceSpine event selected by the existing notifier at max priority. The settings-request min/max consistency rejection does not govern the bot's runtime.

An initial CONFIG_PATHS expansion was removed before final observation: it would have routed missing individual trail fields through the existing readConfig throw. Freezing the existing object directly uses the established deep-copy/hash/freeze producer without extending throw authority.

Additional concrete consumer defect found while exercising zero: raw ratchet distance zero was rejected before the configured minimum distance could apply. Changed the intermediate distance condition from <= 0 to < 0; finite zero now reaches the existing clamp. Both long and short zero-floor fixtures produce the configured 0.3% minimum distance. No default or replacement formula introduced.

Baseline replay changed 389 of 798 existing-policy observations after global reload. Final candidate changes zero. Four actual OrderExecutor entry plans before and four after publication cover explicit stock/crypto and long/short fixture inputs with inert external services. Fourteen one-field-at-a-time actual consumer comparisons affect new policies while old policies remain unchanged. Serialized policy round-trip retains the original hash and all 798 observations. Missing legacy trail emits an alarm; an existing managed stop still triggers its exit.

Full review is NOT complete. Fresh readiness at 2026-09-26T10:22Z succeeded for Mercury and Kimi but Fable returned a live 401 expired OAuth response. Auth status still said loggedIn=true; that is not readiness. Verified process has no CLAUDE_CODE_OAUTH_TOKEN/CLAUDE_CONFIG_DIR override; the existing client strips Anthropic API-key env for its required first-party subscription lane. No account/credentials changed. Asked Trey asynchronously to reauthenticate. Do not run repeated paid readiness checks or substitute another seat without new evidence.

Exact candidate diff shown and git diff --check clean. Final candidate observation: private/candidate-3iRKCC/receipt.json. Exact private patch hash: 7b750c0b0a1926e24b475f8d82d3922e24efdd9c7db2667453ac027acab09a9d. Main inherited source edits remain untouched; no production candidate has been staged or committed.

Next: after reauthentication, run the existing review.cjs with a fresh unique preflight label. Fast-forward the existing clone to the documentation checkpoint so OBSERVATION.json is present as a host-attested source; preserve its four candidate edits. Confirm ten source hashes against OBSERVATION, then execute fixtures/attack.cjs once (7750, no iteration ceiling). Independently disposition all source citations, export full redacted tapes, merge only these scoped changes onto main without overwriting inherited hunks, show exact staged diff, commit/push one connected production change, and repeat from a cold pull. Then continue remaining UI/config families. Do not describe this candidate as delivered or update the delivered 21-field UI contract until it lands.
