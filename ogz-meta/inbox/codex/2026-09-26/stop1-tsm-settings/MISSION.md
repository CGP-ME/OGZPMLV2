# Mission

Trey: “lets keep moving”, continuing the UI-first Stop 1 configuration migration. Existing standing authorization covers completed atomic changes, actual consumer observations, Mercury adversarial review, commit and push. No runtime restart, broker operation, shared indexing, history repair or unrelated dirty implementation is authorized here.

Connect the existing TimeSeriesMomentum configuration to its retained per-symbol instances and owned entry ATR calculation. Expose only the existing consumed inputs, preserving contracts for already-open trades. September walk and current Trey instructions control; historical traces and floating code are leads, not authority.
