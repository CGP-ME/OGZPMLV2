# Work

Final review completed and independently dispositioned in REVIEW.md. Final patch is staged as exactly three production files, 42 insertions / 8 deletions; all eleven observed source hashes match the index. Only this connected RSI2 change and its explicit packet/UI/findings paths are included. Unrelated dirty production hunks remain unstaged. Next operation is the authorized paired commit/push, then fetch the published revision and repeat the actual consumer observation in the existing verification clone. No PM2 or index operation.

Final candidate before delivery: private/final-candidate.patch, SHA256 2a6b6959d1892a92cc396b374f61745683b6ce624904dd624db2a8bb45af7aca. Additional host producer/consumer check found stopLossPercent=-100 accepted by UI but rejected by the actual OrderExecutor entry path (:3179-3181 -> :2127-2146). Reproduced six contract failures at recorded entries. Save metadata now matches existing consumer: strictly above -100 and a negative maximum whose percentage-to-fraction conversion remains nonzero. This is an operator-input bound, not a new runtime gate or stop. Every observed entry now passes the real order stop conversion. Valid -99.9 and the representable upper bound each produce six entries; zero, -100, -101 and subnormal-underflow input are rejected without publication.

Final observation private/candidate-awiqQ9 includes 8 field effects, 51 full coordinator stop exits, 51 max-hold exits, 138 RSI invalidations and four exact threshold/direction checks (99 inclusive for long, not short). Same retained instance and distinct second symbol; old contracts unchanged. Full third review launched on final bytes; prior two model passes do not approve this candidate. Second run also reported selected_seat_unavailable because Mercury primary failed evidence qualification despite a completed recheck. All diagnostic/raw originals preserved; no flags weakened.

Correction before delivery: actual end-to-end RSI2 hint path rejects 99.5 despite the module's looser <100 check. Host reproduced six entry failures after an accepted save, first recorded index 94. Initial no-entry fractional exercise was insufficient. Fix the publication maximum to the existing consumer maximum 99 (inclusive), then exercise actual entries at 99 and reject 99.5 without publication. Do not accept model no_break_found as overriding this behavior. First candidate remains uncommitted; full review in progress is preserved separately.

Candidate is three connected files: RSI2 configure-on-new-snapshot using the already-landed module pattern; actual registered closure forwards current accepted config; eight existing values added to existing editable metadata. Existing request validation gains exclusive bound metadata for the pre-existing strict RSI thresholds. No new runtime throw, strategy gate, mode policy or config values. Baseline actual reload left the retained instance at 11 old entries. Candidate replay: initial 11 / changed 17 (11 shorts), same retained instance and separate second symbol; eight one-field changes affect real entry/contract outputs, 45 stop/max-hold/RSI exit boundary records, old entries unchanged, 12 invalid requests unchanged accepted snapshot, fractional valid thresholds retained. Full chain pending.

Attestation first rejected a long metadata key and slash-joined nonsecret prose as secret-shaped; renamed/rephrased those receipt labels without changing the redactor. One supplemental range exceeded MaxHoldChecker by one line; corrected to actual 55-line EOF. Local attestor then accepted 6/6 sources, 113 prompt lines before the single paid review dispatch. No rejected descriptor was silently counted as delivered.

Starting from delivered managed-stop source 237d8963 plus its cold receipt. Current RSI2 constructor freezes config once; registered per-symbol closure passes only ctx. The settings API has no RSI2 fields. Read actual module, orchestrator registration, ConfigLoader save publication and existing RSI2 exit condition. Next: existing Mercury/Serena blast census and before/after real receiver/orchestrator/policy/ECM observations, then full chain before staging. No production edit yet.
