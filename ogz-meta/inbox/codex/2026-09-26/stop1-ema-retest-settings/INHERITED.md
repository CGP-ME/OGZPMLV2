# Inherited

C009/C010/C011/C015 remain open. EMATrendRetest emaPeriods parser accepts a list and silently filters nonfinite entries; that existing separate input format is not exposed through this eleven-field addition. Confidence formula, configured EMA list, RTH schedule, registration and exit metadata ownership remain unchanged. No claim that targetRR or R-multiple trailing metadata controls the reached coordinator. Preserve main dirty work and protected recovery sources.
