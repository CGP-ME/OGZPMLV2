'use strict';
const fs=require('node:fs'),path=require('node:path'),cp=require('node:child_process'),assert=require('node:assert/strict');
const root=path.resolve(__dirname,'../../../../../..');
const clone=path.join(root,'ogz-meta/inbox/codex/2026-09-26/mercury-exit-receipt/private/cold-pull-5gJPFK');
const label=process.argv[2]||'ema-retest-settings-review';
const evidence=['ogz-meta/inbox/codex/2026-09-26/stop1-ema-retest-settings/OBSERVATION.json:1-1','core/WebSocketManager.js:105-117','core/OrderExecutor.js:2127-2146','core/OrderExecutor.js:3179-3181','core/PolicyBuilder.js:229-244','core/exit/StopLossChecker.js:36-57','core/exit/MaxHoldChecker.js:32-48'];
let question='Mercury, break this EMATrendRetest configuration connection. Scope: the three changed production files and their affected consumers. Use AST/Serena and current source; collect and weigh all candidate findings before deciding. The eleven existing entry/initial-stop/max-hold controls must reach retained, separate symbol instances through the existing settings receiver and accepted configuration. Initial-stop and retest-distance ATR must use the strategy configured period, not silently substituted shared ATR. Accepted edits must satisfy the existing numeric and boolean input domains before publication; existing trades retain their entry policy. Check the actual caller, units, baseline and downstream output. This does not expose targetRR or legacy percent-trailing metadata as proven working controls; confidence formulas, RTH schedule, strategy registration, other exit owners and runtime activation are unchanged. Distinguish inherited defects from this change, cite current source, and retain named coverage limits. Host observations use real isolated production methods on recorded candles plus synthetic stop/hold boundaries, not a running bot. Source excerpts and host receipt follow verbatim:\n';
for(const descriptor of evidence){const[file,range]=descriptor.split(':'),[start,end]=range.split('-').map(Number);question+=descriptor+'\n'+fs.readFileSync(path.join(clone,file),'utf8').split('\n').slice(start-1,end).join('\n')+'\n';}
question+='\nThis is a corrected candidate. Independently checking the previous reviewed version, host found that saving closeAwayAtr=1 and maxExtensionAtr=1 succeeded but makes both entry directions impossible: extension must be <= maximum while confirmation distance must be strictly > closeAway. Actual 500-bar replay then produced zero entries, versus 208 under the declared valid common inputs. The current settings producer rejects equal or reversed bounds before publishing; no runtime gate or entry formula changed. Fourteen invalid requests now leave the accepted fingerprint unchanged, and the valid 0.1 confirmation / 0.2 maximum pair produces 14 entries. Review the corrected source, not the earlier pass, and find any remaining concrete misconnection.\n';
assert.ok(question.split('\n').length<=150);
require(path.join(root,'node_modules/dotenv')).config({path:path.join(root,'.env'),quiet:true});
process.env.MERCURY_CONFIG_FILE=path.join(root,'ogz-meta/inbox/codex/2026-09-24/mercury-ingestion-recovery/private/mercury.isolated.json');
const sources=require(path.join(clone,'trai_brain/mercury-bridge/ask')).resolveEvidenceSources({repoRoot:clone,query:question,descriptors:evidence});
assert.equal(sources.length,evidence.length,JSON.stringify(sources.quarantines));assert.equal(sources.quarantines.length,0);
console.log(JSON.stringify({attestedSources:sources.length,questionLines:question.split('\n').length}));
if(process.argv.includes('--attest-only'))process.exit(0);
const child=cp.spawn(process.execPath,[path.join(root,'ogz-meta/inbox/codex/2026-09-26/stop1-config-connections/fixtures/review.cjs'),label,'--agentic','--reviewers=mercury,fable,kimi','--max-tokens=7750','--attack',...['modules/EMATrendRetest.js','core/StrategyOrchestrator.js','foundation/ConfigLoader.js'].map(p=>'--change-path='+p),...evidence.map(e=>'--evidence-source='+e),question],{stdio:'inherit'});
child.on('exit',code=>{process.exitCode=code;});
