# Review

Full chain `2026-09-26T06-36-35-839Z-e26a3640fb80`: final pass, sourceUnchanged=true, 618,747 input/output tokens across 16 attempts. Provider readiness authenticated mercury-2, claude-fable-5 and kimi-k3. One question/run at a time, max-tokens 7750, no iteration ceiling, explicit three-path ingestion, no indexing or shared storage writes.

Mercury initially alleged duplicate configure syntax, an unaccepted second evaluate argument, launch-profile type mismatch, missing candleSource default and absent validation of runtime channel bounds. Fable checked the source and challenged those claims. Recheck opened Donchian and its registered caller, retracted the two primary allegations and returned no_break_found; Kimi converged on pass. Exact tapes retained, not just the verdict.

Host dispositions: Donchian defines one configure method and its changed evaluate signature accepts config. ConfigLoader save publishes a new frozen object; the actual same-instance and separate-instance receipts show its consumer effect. requiredLaunchProfileBool reads profile JSON, not arbitrary ambient strings. Channel bounds are produced from candles, not UI declarations. The scoped diff changes neither candleSource nor those bootstrap readers. Three JS syntax checks also succeeded. This is a connected configuration repair, not a model-only inference.

Review limitations/corrections: Fable misprinted some ConfigLoader citations as 55xx instead of 25xx. Mercury recheck's 2-of-2 inventory excludes the third changed file; its claim that all relevant lines were read is not adopted. Kimi records that omission. The host read and exercised PolicyBuilder/ECM and the bot-side settings receiver; reviewers did not read those whole consumers. AST counts are syntax candidates, not dynamic coverage. Null transport is preservation of explicitly absent percent fields, not substitution of fallback values. Existing canonical ATR-overlay-off scope and unresolved historical tuning owner are disclosed in the prompt and INHERITED.md. No running-bot, real browser, broker or final UI acceptance follows from pass.
