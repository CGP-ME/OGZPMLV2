# Work

Starting astra-era HEAD 78f21d2e30c8cff155fda243743d03d7ba2aac67, empty index. Prior 32 dirty production/documentation paths remain operator-owned. Existing same-host verification clone fast-forwarded to that exact SHA with clean tracked tree. No new worktree, branch or clone.

Intended cut: reuse Donchian's existing configuration initialization for next-evaluation refresh; forward the accepted configuration through its existing registered caller; expose entryPeriod, atrPeriod, atrStopMult, allowShorts and trailChannelBars through the existing settings API. Keep existing instances and entry contracts. Accept intentionally null percent-trailing fields only for the already explicit channel/ATR trail families; do not change percent-trailing or strategy registration policy. Candidate and behavior proof pending.

Candidate now three production files, 34 additions / 2 deletions; exact diff fixtures/production.patch. Main's related inherited refresh/caller hunks independently match the candidate; unrelated static validation, additional hint fields, other strategy refresh and registration logic stay unstaged. Main's broader null acceptance was narrowed at the touched condition; its unrelated optional-field sets remain preserved outside the commit.

Baseline full orchestrator: 28 null-trailing errors, no entry contracts. Candidate: 28 initial entries; five edits produce 84 entries (39 short signals) on the same instance and on a distinct fixture identity; switching short permission off yields 45 entries, no shorts. Both actual module and entry-policy behavior verified. First candidate exercise had a fixture assertion error: it expected the fanout label SELL_SHORT instead of the ordinary orchestrator output SELL. Corrected the fixture against StrategyOrchestrator:2995; production code was not changed for that assertion. Provider review is in flight, source frozen.

Review completed pass with retractions/dispositions in REVIEW.md; no candidate code changed during review. Additional unchanged-source observation routes saves through the actual bot-side receiver and checks the complete exit coordinator (46 channel-trail / 64 invalidation results). Staging uses the exact three-file patch, not the dirty main files wholesale. The first saved patch omitted a trailing blank context line during export; cached apply --check caught the malformed patch before staging. Corrected the evidence patch only, with candidate source unchanged. Commit/push still pending at this checkpoint.

Delivered code/packet: ed45313ea33815a9350c510ca61e104531a1f425. Push reported 78f21d2e..ed45313e HEAD -> astra-era; ls-remote confirmed that identical full SHA. Three production files changed (34 additions / 2 deletions), plus the own changelog entry, interface/findings updates and required packet. No unrelated floating implementation staged.

Cold verification: reversed only our exact candidate diff in the existing verification clone; tracked tree became clean. Preserved the own untracked OBSERVATION.json in private/pre-pull-attestations, then pulled the code SHA from GitHub. `observe.cjs cold` produced private/cold-pgboRK/receipt.json: same reviewed source hashes, same 28/84/84/45 contract counts, zero errors, 39/39 shorts when enabled and zero when disabled, 46 channel-trail / 64 invalidation coordinator exits, old contracts unchanged. Clone tracked diff remains empty. Dual-hashed redacted cold receipt/console are in tapes/COLD-MANIFEST.json. This is same-host source verification, not independent metal or running-bot acceptance.

Next atomic family: TimeSeriesMomentum's existing settings consumer and owned ATR calculation (C002), using the now-correct channel/ATR null transport. Complete UI inventory, sizing/session/per-trade editing, competing historical ATR overlay (C010) and legacy entry provenance (C009) remain open. No PM2, broker, shared index, deployment or history operation occurred.
