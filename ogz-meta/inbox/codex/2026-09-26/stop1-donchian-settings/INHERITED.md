# Inherited

Main's uncommitted Donchian refresh code is a lead, not accepted wholesale. Its unused static validator and added atrPeriod hint are excluded unless actual consumers justify them. Main's broader orchestrator null/zero, strategy registration and other strategy refresh edits remain outside this cut. TimeSeriesMomentum's shared ATR precedence (C002), legacy missing contracts (C009), final UI inventory and production acceptance remain open. Channel/ATR contracts already declare percent fields null; percent-trailing behavior must not be broadened by correcting transport of that absence.

Existing strategyBehavior.atrContracts is disabled in canonical settings but enabled by an explicit historical tuning profile. StrategyOrchestrator buildAtrContractOverrides and its later Object.assign can replace a strategy-produced risk distance under that profile. This cut does not change that tuning policy or claim all-profile risk ownership. The UI rejects saves into descriptor-owned backtests. The remaining migration must resolve that competing owner before claiming no overrides repository-wide; do not silently treat the profile as the UI's normal path or as automatically authorized policy.
