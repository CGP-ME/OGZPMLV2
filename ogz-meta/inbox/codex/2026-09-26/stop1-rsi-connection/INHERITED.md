# Inherited

Prior dirty code contains broader RSI, sizing, hot-update, state and profit-policy edits. It is not the candidate. C005 null channel/ATR hints and C002 TimeSeriesMomentum ATR ownership remain in the shared FINDINGS.md. Other exit defaults and profit-policy lifetime behavior are not certified by this RSI connection.

The pre-existing ExitContractManager current-default adoption for a trade missing exitContract is reachable through restoration of sufficiently complete older state. It cannot reconstruct the trade's original RSI settings; the candidate does not invent them. Current new-entry producers retain the explicit contract and frozen policy. Do not call restoration certified or add current-setting backfill merely to satisfy a model. Missing-native-frame acquisition is also not repaired by this settings connection; the consuming adapter accepts actual delivered frames and does not manufacture 4h bars from 1h data.
