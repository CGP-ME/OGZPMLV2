# Review

First full-chain review: `2026-09-26T04-54-19-745Z-bc7fbc2bdab9`, final `disagree`, source unchanged. No commit followed. Mercury's duplicate-case syntax error, RSI2-not-instantiated, wrong-period and unused-threshold claims were refuted by actual parsed/executed source. Its claim that this patch added the old `|| 2` fallback was also wrong; the fallback existed, but it was inappropriate for the new RSI route. Fable identified the omitted consumer evidence. Recheck read the actual TradingLoop call supplying priceHistory. Kimi retained the remaining disagreement rather than laundering a pass.

Independent investigation additionally reproduced missing-MA-to-zero behavior and dropped frozen RSI policy fields. The final candidate repairs these and connects the six RSI fields through the existing UI save owner. The implicit period substitution is removed; both current RSI producers deliver their validated configured period. No universal new guard/throw was installed.

Five-file full-chain run `2026-09-26T05-06-15-661Z-c40153935431` ended `disagree`, source unchanged. Mercury's synthesis failed and supplied multiple contradicted claims. Fable corrected the absent-RSI2, condition-name, missing save validation, missing candleSource and unreachable-code allegations. The remaining questions concerned absent 4h input and legacy trades missing contracts. Its recheck incorrectly expected local aggregation and inferred that all RSI exits failed from the base contract alone. Neither conclusion is adopted. C009 in the running findings retains the genuine inherited missing-entry-provenance defect.

Follow-up full chain `2026-09-26T05-17-08-244Z-12310c864cbb` ended `pass`, source unchanged. This was an explicitly focused adjudication on the same candidate's new-entry producer/consumer path, not a replacement claim of broad or deployed coverage. Mercury first alleged an undefined message variable, incorrect strict-comparison behavior and missing producer fields. Fable checked the actual declarations and producers; Mercury rechecked and retracted to `no_break_found`; Kimi converged on `pass`. The original disagreement tapes remain intact. The follow-up's legacy scope boundary is disclosed: unknown restored entry values remain unfixed, not silently cleared.

Host adjudication: `message` is declared at StrategyOrchestrator:1218; strict `>` cannot fire earlier than `>=`; RSI hint construction at :1868-1872 and ECM adoption at :982-993 retain the configured values, independently exercised. A scoped source search across core/, modules/ and the runner found only ECM's internal call at :382 and method declaration at :445 for checkInvalidationConditions. ECM assigns a contract before that call. This proves object presence on that route, NOT the model's overbroad claim that any adopted legacy contract necessarily has RSI fields. Whole-file/tool-count and completeness overclaims are not accepted as executable coverage. No current-setting reconstruction of legacy entry values is approved by this review.

Provider readiness: mercury-2, claude-fable-5, kimi-k3 authenticated; one review at a time, max-tokens 7750, no iteration ceiling. No database indexing. Actual provider usage: first candidate 738,179 tokens; five-file review 2,058,318; focused follow-up 1,517,644. These are cumulative input/output across calls, not a 7,750-total-run budget. Full output, failures, tool provenance and dual-hashed redacted tapes are retained under tapes/.
