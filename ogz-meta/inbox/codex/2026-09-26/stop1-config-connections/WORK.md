# Work

Verified host ogzprime-prod-001, astra-era at 1b7bd188, empty index, 32 inherited dirty tracked files. Reused existing independent cold source pull for exact committed-source review, not a new construction worktree. Main repository remains the authoring/commit owner.

Completed source investigation, two full Mercury-chain runs and direct original/corrected consumer observations. Staged only the three-line ATR ownership correction via fixtures/donchian-atr.patch, excluding the rest of the inherited Donchian diff. A separate changelog hunk records the exact change without staging inherited changelog entries. Baseline/candidate run tracked-byte identities each remained unchanged during review.

The canonical JSON files already exist; this first cut corrects delivery of an existing declared setting rather than inventing another config owner. No claim that the two-file migration is complete. No deployment or index rebuild.

Delivery unit: commit titled `Fixed Donchian ATR setting being overridden by shared indicator`, containing the exact three-line module correction, five-line changelog entry and this packet. Its parent is 1b7bd18839697b8bd8835648eb0154b79374a326; the packet's Git commit identifies the delivered code, and the source hashes in EVIDENCE.md identify the exercised bytes. No inherited API/reconfiguration/exit-hint edits are included.

Next after delivery: C005 nullable channel/ATR exit-hint transport, then C002 TimeSeriesMomentum ATR ownership, each independently reviewed and committed. The running list, not another census, carries findings and rejected model claims forward.
