# Inherited state

32 dirty tracked paths predate this mission. Existing Donchian work includes hot reconfiguration, public validator, ATR calculation and hint changes; none is accepted merely because present. Only independently established corrections will be staged, with exact index diff inspection. Existing throws/validation in the committed constructor are inherited; this connection fix must add no gates, defaults or process authority.

Protected recovery clone, stash, earlier packets and runtime data remain untouched. Historical J-series and proposed settings API restrictions are not implementation authority.
