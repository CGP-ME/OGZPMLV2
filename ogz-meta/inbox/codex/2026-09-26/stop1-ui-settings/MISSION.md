# Stop 1 UI-facing settings

Trey: "now were go on stop 1 migration can we do the ui one first so i can get the final shape over to the ui agent to finish". Authority: current instructions and September walk Part A. UI settings takes priority over the previous exit-hint next action.

First atomic cut: the existing configuration owner's browser read/save connection, targeting a real bot, with only directly verified settings. No new configuration framework, process control, broker action, deployment, or open-trade mutation. The complete settings migration is not claimed by a partial field list. Standing approval covers reviewed atomic commit and push.
