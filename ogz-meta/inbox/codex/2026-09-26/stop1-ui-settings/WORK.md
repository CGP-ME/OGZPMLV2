# Work

Start: astra-era 9070d1cf41e162ed3bca5a0bb76e59a077a76b33, empty index, 32 inherited dirty tracked files. Protected recovery sources unchanged. Existing independent cold-source pull at 87db958a reused for review; no new worktree or branch. Prior dirty UI draft is preserved at private/inherited-before.patch, not accepted wholesale.

Provider readiness, scoped Mercury AST/Serena investigation, candidate full-chain review and direct connection observations completed. Exact production cut is fixtures/production.patch: existing ConfigLoader read/save API for three fields, WebSocketManager request handler, relay target/provenance routing, and runner snapshot/getter connection. No inherited trade-stop, sizing, strategy-refresh, startup-validation, backtest, state, or Mercury edits are staged by this patch.

The inherited overlapping ConfigLoader UI draft was replaced locally with this reviewed three-field implementation; all original bytes remain in private/inherited-before.patch. Other inherited hunks remain in the working tree. The exact committed source is the scoped index candidate, not the entire dirty working file. The expected commit contains the four production paths, one changelog hunk, this packet, and the incremental shared findings update.

UI-CONTRACT.md is usable for the settings transport and first three controls. It explicitly is not the final complete field inventory. Next: separately repair the reproduced RSI producer/consumer mismatch and continue customer setting families before handing Trey a final complete UI shape. No deployment or index rebuild has occurred.
