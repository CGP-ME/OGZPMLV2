# Evidence

Direct receipt-owner observations execute using the exact scoped Git-index JavaScript, excluding the inherited dirty implementation. Command: `GIT_INDEX_FILE=<packet>/private/scoped.index MERCURY_CONFIG_FILE=ogz-meta/inbox/codex/2026-09-24/mercury-ingestion-recovery/private/mercury.isolated.json node <packet>/fixtures/local.cjs`.

`receipt-observation.json` preserves actual ledger/packet outputs for disagreement, missing evidence, identity collision/conflict, failed final reviewer, missing verdict, adverse conclusion and genuine uncertainty. It also replays the two September 25 panel receipts without editing their historical records. `report-omission-observation.json` records one final call with the missing report section retained in the exit receipt and no format-only retry. These are synthetic protocol cases and historical replay, not live model reasoning or bot proof.

The first report-omission fixture used an ignored `fixtures/` source path and produced two malformed citation responses, correctly reporting `citation_target_unattested`. The fixture then used an in-memory policy-eligible source path. The ignore policy was not changed. The later one-call observation concerns report omissions only; actual malformed evidence still receives repair handling.

At the initial a51b33e4 checkpoint, live post-push exercises were pending. The completed runs and the defect they exposed follow; no complete blast-radius coverage is claimed.
# Post-push real Stop1 exercises

Source: independent remote clone at a51b33e4ccafa4a082e503c0423af7aad8805919. Full tracked-file byte/mode/object manifests are in each exported invocation/completion receipt. Submodule gitlinks are explicitly uninitialized, not described as source-read. Dependencies and credentials are shared on this host; no independent-metal claim.

Commands: `node fixtures/cold-exercise.cjs cold-preflight --check-providers`; then `node fixtures/cold-exercise.cjs stop1-volume-baseline --agentic --reviewers=mercury,fable,kimi --max-tokens=7750 --attack --change-path=core/SymbolTradingContext.js <question>` and the same command/question as `stop1-volume-seeded` after applying fixtures/volume-wrong-config.patch in the clone. Exact unabridged arguments are in exported invocation.json receipts. No maximum iteration count supplied. Explicit ingestion uses source snapshots, no database/index/trace-memory retrieval.

Question: "Find a concrete way the VolumeProfile configuration can fail to reach the operative per-symbol VolumeProfile used for trading. This is a bounded Stop 1 configuration-connection exercise, not whole-repository certification. Enumerate and compare the relevant declaration, construction, caller and consumer candidates before deciding; distinguish configured settings, defaults, dormant instances and actual consuming instances. Follow the blast radius beyond the named constructor when the evidence requires it. Report all retained and rejected findings with exact source evidence, why each survives or does not, and any unexamined boundary. Do not execute the bot, providers other than the review seats, or broker actions."

Fresh preflight: mercury-2, claude-fable-5 and kimi-k3 all returned successful matched identities. Baseline raw ID: 2026-09-26t02-05-51-166z-1556488-0f7e6e726024. Seeded raw ID: 2026-09-26t02-10-29-286z-1558711-296579eb2a29. Review reconciliation and honest limits are in REVIEW.md. Historical wrong no_break_found is preserved, not overwritten.

Additional direct observation: `node fixtures/volume-seed-consequence.cjs` invokes the existing cold-pulled VolumeProfile constructor with the two real configured objects; private/seed-consequence.json records all seven expected/observed fields. It does not boot the bot or execute trades.

Correction replay: `NODE_PATH=/opt/ogzprime/OGZPMLV2/node_modules MERCURY_CONFIG_FILE=/opt/ogzprime/OGZPMLV2/ogz-meta/inbox/codex/2026-09-24/mercury-ingestion-recovery/private/mercury.isolated.json node fixtures/replay-cold-decisions.cjs`. Before: seeded ledger/packet no_break_found. After: exact Kimi pass, attributed and explicitly not certification; baseline disagree unchanged; all original answers and original ledger bytes unchanged. This replays actual receipt owners, not another provider run. Two initial fixture invocations omitted required credential bootstrap and failed on the existing embedding-config import before any provider call; fixture now uses the existing process-only dotenv bootstrap, without printing/copying credentials.

Tapes: cold-pull-tapes/MANIFEST.json identifies every original path/hash, redacted plaintext hash, and compressed committed hash. Files include provider outputs, original ledger, evidence text/manifests, complete console logs, source manifests, invocation/completion and the direct/replay observations. Redaction uses the existing redactSensitiveText function; original private files remain untouched. No Jest or bot boot was used as delivery proof.

Export verification: 63 of 63 gzip and redacted-content hashes rechecked, 1,989,776 compressed bytes. No matches for the loaded nonempty credential values or email-shaped text. A separate credential-pattern scan produced six file hits, all traced to two historical `risk-...` documentation filenames in each source manifest (the regex began inside the word `risk`), not credentials. This bounded check is not a universal secret-detection guarantee.
