# Evidence

Direct receipt-owner observations execute using the exact scoped Git-index JavaScript, excluding the inherited dirty implementation. Command: `GIT_INDEX_FILE=<packet>/private/scoped.index MERCURY_CONFIG_FILE=ogz-meta/inbox/codex/2026-09-24/mercury-ingestion-recovery/private/mercury.isolated.json node <packet>/fixtures/local.cjs`.

`receipt-observation.json` preserves actual ledger/packet outputs for disagreement, missing evidence, identity collision/conflict, failed final reviewer, missing verdict, adverse conclusion and genuine uncertainty. It also replays the two September 25 panel receipts without editing their historical records. `report-omission-observation.json` records one final call with the missing report section retained in the exit receipt and no format-only retry. These are synthetic protocol cases and historical replay, not live model reasoning or bot proof.

The first report-omission fixture used an ignored `fixtures/` source path and produced two malformed citation responses, correctly reporting `citation_target_unattested`. The fixture then used an in-memory policy-eligible source path. The ignore policy was not changed. The later one-call observation concerns report omissions only; actual malformed evidence still receives repair handling.

Live post-push exercises remain pending. No Stop 1 readiness or complete blast-radius coverage claimed.
