# Work

Start: astra-era at d1ef0b1628b56c650d82724a14ffd79ce512f9c2, empty index, 32 inherited dirty tracked paths. No source changes yet in this mission. Prior preservation clones and stash untouched.

Fable readiness at 2026-09-26T00:58:56.987Z: actual claude-code response PROVIDER_OK, applied claude-fable-5, matched identity, one request, no tools, successful termination. No credential output.

Historical origin checked locally: 89c5ffe4 (August 28) failure handling; ba6be563 (August 31) panel ceiling; 1d1d3364 (September 1) doctrine/report caps. These are Mercury review machinery, not bot-stop instructions.

Implementation: panel diagnostics no longer own or replace verdicts; exit reports identify the last selected reviewer's actual declared conclusion and preserve all prior seat positions. Missing/failed final seats cannot become an earlier pass. Doctrine findings, evidence coverage, identity observations and failures remain recorded; recheck assessments are now included in ledger summaries and terminal receipts. Removed the format-only final-report retry. Repository/executable/authentication boundaries unchanged.

Six production files form this one logical receipt-policy change. A separate temporary index excludes existing claim transport, temperature, AST and search changes. No worktree or branch was created. The scoped patch is verified against HEAD and the actual receipt functions exercised from that index. The real index was initially empty.

Committed and pushed a51b33e4ccafa4a082e503c0423af7aad8805919, `Fixed Mercury verdict caps replacing reviewer receipts`. Push reported d1ef0b16..a51b33e4 on astra-era; a subsequent `git ls-remote origin refs/heads/astra-era` returned that exact full SHA. Remaining inherited changes are unstaged and preserved.

Independent remote shallow clone: private/cold-pull-5gJPFK at the same SHA. Post-push exercise uses this clone, not the dirty working implementation. Scope is the VolumeProfile per-symbol configuration connection. No index rebuild or bot operation. Installed dependencies and provider credentials are reused on this host: this is a cold source pull, not independent metal.

Two actual full-chain runs completed on a51b33e4: baseline `2026-09-26T02-09-13-070Z-0dabcb61823d`, then seeded `2026-09-26T02-13-00-248Z-34448cdfcba4`. Both exit 0; exact tracked-byte manifests before/after agree within each run. The second differs from HEAD only by fixtures/volume-wrong-config.patch, applied only inside the private clone. The active bot tree was not seeded.

The seeded run exposed my reporting defect: all reviewers agreed the wrong configuration owner was a break, while Kimi's role-specific `pass` meant agreement. reportedPanelDecision incorrectly renamed this to no_break_found. Correction: preserve the exact raw declared verdict. No new ceiling, gate, retry, fallback or bot behavior. The original wrong ledger remains intact. fixtures/replay-cold-decisions.cjs reproduces the old and corrected ledger/packet headers against both live runs, preserving every original seat answer. Earlier synthetic expectations that pass meant no_break_found were wrong and are corrected rather than treated as authority.

The same vocabulary mismatch also made the label-comparison diagnostic claim reviewer_disagreement. It now accurately says reviewer_verdict_labels_differ, verdictLabelsMatch and sharedVerdictLabel. The terminal explicitly distinguishes label equality from semantic consensus. Full reviewer accounts of substantive disagreement remain unchanged; no host consensus is manufactured.
