# Review

The authorized removal means missing evidence or mismatched reviewer identity no longer automatically rewrites the model's verdict. These remain visible diagnostics, not a certification that the model is correct. No weaker executable/auth/repository boundary is authorized. Check that failed or absent reviewers cannot be represented as having supplied a successful answer, and that old answers, failed attempts and disagreements remain in the exit receipt.

## Live results and correction

Two of two planned full-chain exercises completed; this is not a whole-Stop1 coverage denominator.

- Baseline: Mercury initially inferred a missing configuration without tracing its producer. Fable challenged the insufficient evidence. A 23-iteration Mercury recheck expanded to real callers and consumers, but retained a hypothetical absence instead of establishing it. Kimi disagreed. Some Kimi "unread" claims are demonstrably wrong: it was supplied VolumeProfile lines 1-260 and runner lines 770-820, which contain the supposedly unread constructor and line 799. These are model errors, not missing source or permission to add configuration gates.
- Seed: one line routes maDynamicSRConfig to VolumeProfile. Mercury identified the exact wrong-owner argument; Fable confirmed against host source/diff/reference/AST receipts; Mercury's 11-iteration recheck used real source and Serena property-reference tools; Kimi agreed that a break existed. A separate direct call of the existing VolumeProfile constructor confirmed the correct owner supplies seven of seven configured fields and the wrong owner supplies zero of seven. This is a local constructor observation, not bot runtime proof.
- Reporting defect caught: the seeded ledger said no_break_found because the receipt owner renamed Kimi's agreement `pass`. The one-line correction removes that translation. The replay demonstrates both ledger and human packet now preserve `pass` attributed to Kimi, while Mercury/Fable found_break and Kimi's explicit agreement on the defect remain intact. `pass` is not a claim of defect-free code.

Ready: actual provider chain, file-backed evidence, parser/reference tools, source rechecks and detection of the seeded configuration-owner error. Not proven: exhaustive configuration migration coverage, complete/reliable candidate adjudication, runtime delivery, or Stop1 completion. Missing producer tracing and inaccurate reviewer coverage claims remain real limitations; removing UNVERIFIED does not repair those.
