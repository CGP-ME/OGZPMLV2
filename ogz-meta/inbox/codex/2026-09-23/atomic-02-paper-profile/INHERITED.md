# Inherited

Both canonical config files and their loader predate this correction. This commit is not full Stop 1/configuration migration completion. Pending settings API, startup checks, sizing, strategy, trade-stop and other changes remain outside its scope.
