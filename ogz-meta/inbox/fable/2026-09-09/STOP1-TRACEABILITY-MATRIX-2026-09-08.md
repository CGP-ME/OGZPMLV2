# STOP 1 — TRACEABILITY MATRIX
## Trey's exact requirement → execution instruction → code paths found → evidence that checks it

**Status vocabulary (Sol, Sept 8):** every row's evidence cell carries one of `OBLIGATION` (what must be produced), `EXECUTED` (produced, not yet passed), `PASSED` (produced and passed). As of Sept 8 every row is **OBLIGATION** — nothing has run. Code paths are marked `TRACED` (call chain read) or `CANDIDATE` (sweep hit, not confirmed); a heading that said "every relevant path" overclaimed for the CANDIDATE rows and is changed. Original finding IDs are preserved in STOP1-RECONCILIATION-B01-B21-2026-09-08.md; this matrix does not close any of them.

Companion to STOP1-BOUNDARY-MISSION-v2. Frozen code `e54a8b8`. A row with an empty fourth column is not executable. "Part A" = OGZ-WALK-SPEC Part A (Trey verbatim). "v2" = STOP1-BOUNDARY-MISSION-v2. Code paths are the full set found by the Part 5 sweep, not the first one found.

| # | Trey's exact requirement (Part A / dated ruling) | Execution instruction (v2) | Code paths found (e54a8b8) — TRACED unless marked CANDIDATE | Evidence that checks it — status |
|---|---|---|---|---|
| 1 | A0: "all of the values that are loaded into the bot at the beginning that all of them are true and all of them are in the right config and only one config that there's no silent overrides anywhere" ; Sept 4–5: two files (settings / internals) + `.env` keys; Sept 8: loader owns zero values | v2 §1.1–1.3: settings.json / internals.json / `.env` credentials; one `load()`; no defaults on the trade path; one owner per leaf; refusal on legacy behavioral names | `foundation/ConfigLoader.js` 26 (dotenv), 392-444 (parsers), 485-527 (merge), 548-640 (profile), 853-880 (legacy env→path), 1478-1545 (snapshot), 2219-2238 (profiles validator), 2750-2810 + 3894-3982 (BASE_CONFIG / compat surface), 4373-4380 (getAll/getExitContract); `ecosystem.config.js` 16-35 (dotenv pre-read), 77-196 (stamps); `config/features.json` via `core/FeatureFlagManager.js:114`; `PatternMemoryBank.js:161`, `UnifiedPatternMemory.js:184` (runtime re-loads); `utils/telegramNotifier.js:44`, `utils/discordNotifier.js:48` (own dotenv); direct env readers per manifest + `claudito-logger.js:327-403`, `TRAIDecisionModule.js:90`, `NtfyTraceNotifier.js:194-201`; 179 `\|\|` fallbacks (v2 Part 5) | OBLIGATION — Manifest 0.1c (every leaf one destination); consumer-effective parity table (v2 §1.5); `process.env` reads on runtime path = 0 outside loader + credentials; refusal receipt with a real legacy name; boot after migration. 179 fallbacks in col. 3 are CANDIDATE. |
| 2 | Sept 3/4: multi-direction, one trade per asset (Ruling 1) | v2 Part 2 item 4: paper `pipeline.directionFilter = "both"` — gate only | `ConfigLoader.js:1041` (profile-only read); `TradingLoop.js:692` (sell entries blocked when long_only); five strategies `allowShorts=false` (`strategies.*` blocks); `exitContracts.*.maxConcurrentEntries` (NoWickImbalance = 2) | OBLIGATION (no evidence yet) — Boot receipt shows `directionFilter=both`; packet lists the five `allowShorts=false` and the eight unverified short paths; one-trade-per-asset **no evidence yet** (stop 3/4) |
| 3 | A1: auto-switch stocks/crypto; "30 minutes out not taking any new trades by fifteen minutes… intentionally flattening… by 5 minutes… sell everything"; A9: configurable sessions | v2 R2 — Trey's word; if (b) the paper profile stays static/stocks through stop 1 | `ConfigLoader.js:572-590, 665-683` (scheduled requires `schedule`); `run-empire-v2.js:863-876` (no schedule passed); `SessionRouter.js:47-76` (no schedule input), 341-351 (market-calendar phase), 416-460 (30 soft_stop / 15 warn / 5 force_flatten) | OBLIGATION — R2(a): schedule contract consumed end-to-end + 15-min flatten trace + wind-down fixture at 30/15/5 showing the ruled actions. R2(b): **deferred, no evidence claimed**; recorded as stop-8 opening facts |
| 4 | A10: "paper on launch always" | v2 R3 — Trey's word | `ConfigLoader.js:548-610` (profile selection), 688-717 (mode derived from profile); `trading.config.json:20-32` (production profile mode live, confirmLive true) | OBLIGATION (no evidence yet) — Boot with production profile saved and no launch confirmation → paper + named reason; **no evidence yet until R3 ruled** |
| 5 | Sept 6: "loader refuses to run live unless the guards are on? … rip it out" | v2 Part 2 item 2: delete `ConfigLoader.js:1250-1254` | `ConfigLoader.js:1250-1254` only | OBLIGATION — Diff shows only that condition removed; boot with live profile + guards off proceeds (paper box test can only exercise the validator, not live) |
| 6 | Sept 6: cooldown "none and take that shit out for the same reason" | v2 Part 2 item 3 | `entryLogic.symbolLossCooldown` (JSON), pm2 `SYMBOL_LOSS_COOLDOWN_*` stamps, `StateManager.js:1676, 3973-3979` and their callers | OBLIGATION — Grep for the reader = 0; manifest rows DELETE-RULED; boot |
| 7 | Sept 4/6: "nothing shuts anything down or stops the bot except dd during eval"; Fourth Shape: fails loud, never process death | v2 Part 0 #2 / Part 2 item 1: one handler set, remove runner bootstrap + SingletonLock fatal handlers; R5 (lock monitor); R6 (symbol halts) | `run-empire-v2.js:316-328`, `3628-3645`; `SingletonLock.js:144-166` (fatal), 175-185 (lock monitor exits), 147 (SIGINT/SIGTERM exit 0 = operator); `run-empire-v2.js:3586` (shutdown exit 0), 3655; `OrderRouter.js:411-440` + `OrderExecutor.js:2965-2992` (never-clearing broker-truth block, stop 2); `StateManager.js:4115` (haltSymbol, no clear) | OBLIGATION — Registered-listener list after boot = one per event; rejection fired → alive + trace + buzz; R5/R6 ruled and their receipts; the lock-monitor exit tested if R5(a) kept |
| 8 | Sept 6: warnings at 3/4/5 losses in a row and 5/10/15% down; Sept 6: 50% daily-loss timeout, halt new entries only, nothing sold, user restarts | v2 §1.4: settings rows exist; implementation stop 8 | **No consumer exists** (Part 5 sweep: no reader of these names; RiskManager guardMode=off) | OBLIGATION (no evidence yet) — Settings rows present in settings.json; implementation **no evidence yet** (stop 8) |
| 9 | Sept 7: keep the track-record publisher | v2 R1 — Trey's word on default | `ogz-meta/claudito-logger.js:292-431, 580-630, 717-729`; imported `run-empire-v2.js:382`; publisher inputs incl. `STARTING_BALANCE`, `BROKER`, `OGZ_MAX_DRAWDOWN`, `OGZ_PROFIT_TARGET`, `TTP_MAX_LOSS_DOLLARS` | OBLIGATION — Nine+ inputs as settings rows; publisher enable per R1; if R1(a): its caught failure at `:717-729` routed to trace/buzz (item 1 inventory) |
| 10 | Sept 7: keep and wire the PID; "PID + pattern learning = ML layer" | v2: PID rows → settings; wiring is stop 4 | `core/PIDController.js`; fed at `OrderExecutor.js:4901, 5530`; output consumers **none found** (Codex direct audit) | OBLIGATION (no evidence yet) — Settings rows; wiring **no evidence yet** (stop 4) |
| 11 | Sept 7: circuit breaker — Fourth Shape, delete | v2 Part 2 item 11 | `config/features.json` CIRCUIT_BREAKER block (no reader); `core/ErrorHandler.js` (zero runtime references) | OBLIGATION — Reference search = 0 before delete; boot after |
| 12 | Sept 8: "there's only one fee structure because alpaca doesn't charge fees" | v2 Part 2 item 13: per-venue fee block; Alpaca = 0 (ruled value change) | `FeeModel.js:32-58` (one global read), `OrderExecutor.js:2063-2074`; pm2 `FEE_*` stamps; `feeProfiles` in JSON | OBLIGATION — Exception ledger entry; paper P&L on Alpaca after the cut carries zero fees (first trades' receipts) |
| 13 | Sept 8: revisioned settings — "do it" | v2 §1.7 + R4 | `ConfigLoader.js:1437-1477` (fingerprint, blind to nested); `PolicyBuilder.js:561-616` (frozen policy fields); `ExitContractManager.js:263, 729, 821, 836, 948` (global reads after entry) | OBLIGATION — `settingsRevision` + hash on a receipt; nested-value change → hash changes; R4(a): exit consumers read the frozen policy (test: change global trail config mid-trade, open position unaffected); R4(b): A21 marked PARTIAL with those five lines listed |
| 14 | Sept 6 (from the box): phone must not be silent — named absence law | v2 Part 2 item 6 | `run-empire-v2.js:1063-1082` (notifier factory, silent when topic missing); `NtfyTraceNotifier.js:40-125` (keyword filter — stop 2) | OBLIGATION — Boot without `NTFY_TOPIC` → printed line + `NTFY_NOTIFIER_ABSENT` trace; boot with it → notifier installed line |
| 15 | Doctrine: "labels, headers, docs… are claims, not proof" — boot must not lie | v2 Part 0 #1 (required-module honesty), #3 (exit code), item 12 (cosmetics) | `ModuleAutoLoader.js:136-283` (required list incl. absent `OptimizedTradingBrain`; `validateModules` never called); `run-empire-v2.js:429-437` (lock name — NOT cosmetic, unchanged), 1910-1927 + 3586 (failed start exits 0); banner/checkpoint prints | OBLIGATION — `validateModules` receipt; forced start failure exits ≠ 0; hold path alive + named; diff shows lock name untouched |
| 16 | Sept 7: Mercury reads everything before it answers; seats write what they did and why | Bridge mission Parts A–C + receipt commit (landed `66d852fc…b6bbd44c`) | `trai_brain/mercury-bridge/*`; **shared runtime file** `core/persistent_llm_client.js` (two commits) → `trai_core.js:55, 126-134` | OBLIGATION — Ledger 2026-09-08.jsonl:4 FULL; seat self-report fields present on a new run; Part 3 of v2 records the shared-client change honestly |
| 17 | Sept 8: "check for the adjacent bug class types… missed twice or a timebomb" | v2 Part 5 (nine classes swept) | Listed per class in Part 5 | OBLIGATION — candidate sets exist with counts and file:line (CANDIDATE); reference-level confirmation of the 179 fallbacks and 9 validators not yet produced (Mercury job) |

Rows with "no evidence yet" are not defects in the matrix; they are the honest state, and each names the stop or ruling that produces the evidence.

---
**WHAT I DID:** built the matrix from Part A, the dated rulings in the walk spec, v2, and the code paths I read this session plus the Part 5 sweep.
**WHAT I DID NOT DO:** read the strategies' short-path code (row 2), the warnings/timeout consumers (row 8 — none exist), PID output consumers (row 10 — Codex's audit says none).
**WHAT I ASSUMED:** nothing; every "no evidence yet" is stated as such.
