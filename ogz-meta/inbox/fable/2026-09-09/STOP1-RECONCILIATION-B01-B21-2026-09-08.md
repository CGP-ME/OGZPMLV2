# STOP 1 — RECONCILIATION OF SOL'S FINDINGS (B01–B21, D1, D2) → STATUS

Rule (Sol, Sept 8): every original ID keeps its number; each ends in exactly one of — **CORRECTED** (v2 instruction, cited), **COUNTEREVIDENCE** (finding withdrawn with receipt), **DEFERRAL PENDING TREY** (an R-item; naming a deferral does not authorize it), **STILL OPEN**. Reorganizing findings into new tables never closes one.

Nothing below is executed. Every CORRECTED item is a corrected *instruction*; execution status for all of stop 1 is OBLIGATION until the cut runs.

| ID | Finding (short) | Status | Where / receipt |
|---|---|---|---|
| B01 | Per-commit acceptance contradicts commit separation | **CORRECTED** | v2 Part 0: ordered table, acceptance defined per commit against the state it creates. Order: 0 manifest 0.1c → 1 required-module list → 2 handlers → 3 exit code → 4 migration → 5–13 ruled changes → 14 revision contract. No commit tested against a later commit's capability. |
| B02 | SingletonLock registers fatal handlers that exit | **CORRECTED** | v2 Part 0 #2 / Part 2 item 1: remove `run-empire-v2.js:316-328` AND `SingletonLock.js:157-166`; SIGINT/SIGTERM cleanup `:144-154` kept; receipt = registered-listener list after boot. Lock-monitor exits `:175-185` → **R5** (separate class). Verified by Fable: `SingletonLock.js` read in full. |
| B03 | `scheduled` needs a schedule the runner doesn't pass | **DEFERRAL PENDING TREY (R2)** + strengthened | Fable read: loader requires `schedule` (`ConfigLoader.js:587-588`); runner passes none (`run-empire-v2.js:863-876`); `SessionRouter` constructor has **no schedule input** (`:47-76`) and routes on market-calendar phase (`:341-351`). The validated object is dead. R2(a) implement / R2(b) defer to stop 8. |
| B04 | 15-minute wind-down is a warning, not the flatten Trey specified | **DEFERRAL PENDING TREY (R2)** | `SessionRouter.js:421-457`: 30 soft_stop (pause), 15 warn (pause), 5 force_flatten. A1 says flatten at 15. Ships only with R2(a) and a fixture proving the ruled actions. |
| B05 | Two brokers, one fee model | **CORRECTED by ruling** | Trey Sept 8 (A22): Alpaca charges nothing. v2 item 13: per-venue fee block, Alpaca=0 as a ruled value change in the exception ledger; venue selection code when a second broker is active. `FeeModel.js:32-58`, `OrderExecutor.js:2063-2074` read by Fable. |
| B06 | Snapshot parity ≠ consumer-effective parity | **CORRECTED** | v2 §1.5: parity over consumer-effective values per row (get / compat / BASE_CONFIG / getExitContract); baseline = box process env captured privately; secrets never in receipts; structural repairs recorded separately. |
| B07 | "Keep one sizing block" too broad | **CORRECTED with mapping** | Fable read Sept 8: live controls — `positionSizing.basePositionSize` (readers TradingLoop:2303, ModuleInitializer:51 via alias `sizing.basePositionSize` ← env `BASE_POSITION_SIZE`, ConfigLoader:787/1592/2019), `positionSizing.maxPositionSize` (OrderExecutor:3107/3118/3919/4187, PositionSizer:28 via alias ← `MAX_POSITION_SIZE_PCT`, :788/1593/2020), `positionSizing.maxPositions` (TradingLoop:1451/1499/1527 ← `MAX_POSITIONS`, :789/1594), `entryLogic.sizing.absoluteCapPercent` (OrderExecutor:2064 ← `ABSOLUTE_POSITION_CAP`, :2023) — four leaves, one owner each after the cut. Dead by literal path: `entryLogic.sizing.basePositionSize`, `entryLogic.sizing.maxPositionSize` → DELETE with this receipt (0.1c). v2 §1.2 forbids block choices. |
| B08 | Leaf filter omits required empty arrays | **CORRECTED with count** | `leaf-diff.py` (audit skill) represents empty containers as rows; run on `39c04cc5`: **11 missing** — 5× `launchProfiles.*.strategies.soloFilter` (loader-required, `ConfigLoader.js:149-163, 204-213`), 4× `exitContracts.*.invalidationConditions`, 2× `exitContracts.*.scaleIn.addSizingLadder`. 0.1c adds them. v2 §1.5 structural parity. |
| B09 | 341 HOLDs + bundled rows; completeness overclaimed | **CORRECTED** | v2 §1.2: every HOLD ruled or listed RETAINED-IN-PLACE with owner and stop; bundled rows (M0001, M0044, L0039, …) split in 0.1c; no leaf moves as a bundle. |
| B10 | Env denominator / credential list incomplete; independent `.env` loads | **CORRECTED** | v2 §1.1 credential surface + Telegram/Discord; §1.3 denominator adds `claudito-logger.js:327-403` inputs, `TRAIDecisionModule.js:90`, notifier modules; the two `dotenv.config()` calls (`telegramNotifier.js:44`, `discordNotifier.js:48`) removed. Sweep class 3 confirms exactly 3 loads. |
| B11 | Refusal must map legacy env names to canonical paths | **CORRECTED** | v2 §1.3 refusal: explicit `legacyEnvName → path` map from `ConfigLoader.js:853-880` + census; receipt uses a real legacy name. |
| B12 | KEEP rows preserve zero-swallowing fallbacks (FVG) | **CORRECTED + class swept** | Explicit discharge: `FairValueGapDetector.js:25-27` `config.minFVGPercent \|\| 0.05` → bind to the canonical setting, `??`/explicit-missing handling, valid 0 preserved; M0081/M0082 KEEP withdrawn (0.1c). Class swept: **179** `\|\| <number>` config-fed fallbacks in 15+ files (v2 Part 5) — all become manifest rows; each dispositioned, none grandfathered. Reference-level confirmation of each: **STILL OPEN** (Mercury job). |
| B13 | Revisioned settings not specified | **CORRECTED (contract) + DEFERRAL PENDING TREY (R4 scope)** | v2 §1.7: revision/hash on every save, activation at next boot, `settingsRevision`+hash on every receipt. |
| B14 | Exit consumers read globals, not the frozen policy | **DEFERRAL PENDING TREY (R4)** | `ExitContractManager.js:263, 729, 821, 836, 948` read global after entry; `PolicyBuilder.js:561-616` freezes contract/profit/fees only. R4(a) rewire now / R4(b) stop 6 with A21 marked PARTIAL. |
| B15 | PROFILE placement conflict; "paper on launch always" unresolved | **CORRECTED (placement) + DEFERRAL PENDING TREY (R3)** | 0.1c: E0114 → SETTINGS, E0041/E0111 → BOOTSTRAP. R3: semantics of A10 with a saved live profile. |
| B16 | Publisher default-off was an unratified addition | **COUNTEREVIDENCE on authority — it was Fable's, not Trey's** → **DEFERRAL PENDING TREY (R1)** | v2 R1. |
| B17 | Global handlers don't cover producer-caught errors | **CORRECTED** | v2 item 1: caught failures (`claudito-logger.js:717-729`, `NtfyTraceNotifier.js:175-201`) inventoried separately, named absences on stop 2/7 sheets; not claimed fixed. Item 11's coverage claim narrowed. |
| B18 | Exit-code change must not convert holds/degradation into termination | **CORRECTED** | v2 Part 0 #3: only `start()` catch → `shutdown()` → `exit(0)` (`run-empire-v2.js:1910-1927, :3586`) changes; hold (`:1800-1840`) and TrAI-degraded (`:1841-1849`) unchanged; boot receipt states constructed/initialized/connected/held/entries-enabled. |
| B19 | Honest required check exposes stale `OptimizedTradingBrain` | **CORRECTED** | v2 Part 0 #1: list reconciled from actual dependencies; `validateModules` (`ModuleAutoLoader.js:267-283`, never called today) wired. Fable verified file absent at e54a8b8. |
| B20 | Lock name is not cosmetic | **CORRECTED** | v2 item 12: `ogz-prime-v14` unchanged; a rename is its own ruled change (`SingletonLock.js:10-18, 46-82`). |
| B21 | Two bridge commits touch a shared runtime client | **CORRECTED (record)** | v2 Part 3: `5640f7fd`, `c51e76c2` touch `core/persistent_llm_client.js` (TrAI path `trai_core.js:55, 126-134`); ceiling change ≠ configured timeout change (`trai_llm_config.js:54-82`). v1's "touches nothing the bot runs" withdrawn. (Fable did not read `trai_core.js`/`trai_llm_config.js` — relies on Sol's cited lines.) |
| D1 E0041 | `DOTENV_CONFIG_PATH` kept in `.env` | **CORRECTED** | → BOOTSTRAP class (0.1c). |
| D1 E0114 | `PROFILE` kept in `.env` | **CORRECTED** | → SETTINGS (0.1c), matching v2 §1.1. |
| D1 M0042/M0043 | BreakAndRetest cadence / top-10 levels as KEEP | **CORRECTED** | → RULING-NEEDED (0.1c); decision-affecting per Sol's read of `BreakAndRetest.js:133-150, 266-285` (Fable did not open this file). |
| D1 M0081/M0082 | FVG fallbacks as KEEP | **CORRECTED** | see B12. |
| D1 Q0132/Q0133 | CIRCUIT_BREAKER description/version → settings | **CORRECTED** | → DELETE-RULED with the block (0.1c). |
| D2 E0111 | `NODE_ENV` note | **CORRECTED** | BOOTSTRAP, pm2 permitted (0.1c). |
| D2 E0189 | `INCEPTION_API_KEY` as tooling-only | **CORRECTED** | credential surface; reader `trai_llm_config.js:54-82` (Sol's cite) (0.1c). |
| D2 E0190 | `STARTING_BALANCE` as backtest-only | **CORRECTED** | live publisher input (`claudito-logger.js:375-431`); owner map required (0.1c). |
| Sol "DELETE status" | 41 dead rows not independently re-proven by Sol | **STILL OPEN as an independent proof; three prior methods stand** | Fable grep + Amp AST + Codex direct audit agree on the 41; Sol did not re-run. Mercury reference pass is the fourth. Unflagged rows gain nothing by omission. |

**Items Sol did not raise, found by Fable's class sweep (Sept 8), added to v2 with the same status discipline:** dead `schedule` leaf (strengthens B03, R2); lock-monitor exits (**R5**); symbol halts never clear (**R6**); 9 validators with zero direct callers (CANDIDATE, per-stop rulings); `pm2 delete` + `pm2 start ecosystem.config.js --only` on ignition (v2 Part 4).

**Execution status of everything above: OBLIGATION.** Nothing has been cut, booted, or probed on the box against v2. "We know what must be tested" ≠ "we tested it" ≠ "it passed" — Sol's line, adopted as the matrix's status vocabulary.

---
**WHAT I DID:** mapped every B/D ID to its v2 line or R-item; ran `leaf-diff.py` on `39c04cc5` for B08; traced every sizing leaf's readers for B07; re-read `SingletonLock.js`, `ModuleAutoLoader.js`, `SessionRouter.js` (cited ranges), `FeeModel.js`, `OrderExecutor.js` (cited ranges), notifier modules.
**WHAT I DID NOT DO:** execute any obligation; open `BreakAndRetest.js`, `FairValueGapDetector.js`, `trai_core.js`, `trai_llm_config.js` (Sol's citations relied on, marked as such).
**WHAT I ASSUMED:** nothing.
