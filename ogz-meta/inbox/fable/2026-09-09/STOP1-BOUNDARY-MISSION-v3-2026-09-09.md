# STOP 1 BOUNDARY — MISSION SPEC v3 — CONFIG CUT + RULED CHANGES

**Supersedes:** v2 (2026-09-08). v2 received a DO NOT FIRE from Codex's attack (Sept 9, on the box); every attack row is addressed by ID (C-01…C-19) in STOP1-RECONCILIATION-ATTACK-v2-2026-09-09.md. v1 and v2 stay in the tree as history. All twelve code claims in the attack were re-verified by Fable at `e54a8b8` before this revision.
**Frozen code:** `e54a8b8`. **Manifest:** `39c04cc5` (0.1b) + 0.1c corrections below. **Records:** `f99a7a22`.
**Branch:** one branch, `codex/multi-asset-symbol-state`. Code frozen until this boundary; packets land as produced.
**Status:** HOLD. All rulings R1–R8 are in (Sept 9). **Standing rule (Trey, Sept 9): anything phrased as "defer till later in the build" is do it now — full shape, all possibilities assumed.** Fires only after: 0.1c lands and is cold-pulled by both readers; a second seat attacks v3; Trey names the executor and says go. **Landing is not ignition (C-19):** a separate, explicit PM2 activation approval from Trey is required after cold-pull; landing code never triggers a PM2 delete/start.

Every line is a Trey ruling (RULED, dated), a Sol finding (Bnn/Dnn), or a mechanical consequence. Nothing is the executor's judgment. Anything unclear → the executor stops and asks.

---

## R — RULINGS TREY MUST MAKE BEFORE v2 FIRES

**R1. Publisher — RULED Sept 9: off.** Trey walked the nine values and ruled "just turn this off." `trackRecordPublisher.enabled=false` in settings (now a Trey ruling, not Fable's addition); the nine inputs become settings rows, empty, named absent in the receipt. When it is turned on later: balance is read from the broker, not typed; profit target and drawdown point at the existing settings leaves rather than being typed twice (Trey, Sept 9). The caught failure at `claudito-logger.js:717-729` is inventoried under item 1 for that day.

**R2. Switching — RULED Sept 9: build it now.** Read end to end Sept 9 (`SessionRouter.js:275-545, 1003-1113, 1481-1576`). What exists: the transition already pauses, force-closes the source side through execution, REST-reconciles both brokers, hands the pattern memory to the target bank by mode+asset and refuses on mismatch (`:1057-1113` — the five-bank rule is enforced on this path today), unsubscribes, registers the target broker, resubscribes, resumes. What does not exist: **a schedule.** The router counts down only at RTH open (crypto→stocks during phase `pre`) and RTH close (stocks→crypto during RTH) from a hardcoded market calendar (`:387-414`); `ConfigLoader` validates a `schedule` object (`:587-588`) that nothing consumes. Item 5, commit 14: (i) a schedule contract — the profile's `sessionRouter.schedule` (Trey's default: stocks 09:30–16:00 ET, crypto otherwise; configurable blocks per A9) passed by the runner (`run-empire-v2.js:863-876`) and consumed by `_targetSessionFromPhase`/`_windDownCountdown` in place of the hardcoded calendar; (ii) wind-down per A1: 30 min = no new entries, **15 min = intentional flatten at market** (today `warn` at `:424-428`), 5 min = sell everything; (iii) fee selection by venue per item 13; (iv) the paper profile flips from `static/stocks` to switching. Candidate bug found on this read, for the packet: `_windDownPauseScope` (`:532-544`) scopes the wind-down pause to `symbols[0]` only — the executor confirms whether StateManager applies the pause per symbol and, if so, scopes it to every symbol of the session. Proof: wind-down fixture at 30/15/5 showing the ruled actions; one real RTH boundary on paper in the boot-after-commit run. Item 4 (shorts gate) unchanged.

**R3. "Paper on launch always" — RULED Sept 9: paper, forever, on boot.** Read Sept 9: execution mode is consumed in **59 places across 17 files** (run-empire 17, StateManager 9, WebhookOrderAdapter 5, BotStateFrame 4, UnifiedPatternMemory 3, RuntimeConfigProof 3, PatternMemoryBank 3, FeatureFlagManager 3, SessionRouter 2, OrderExecutor 2, CryptoMarketFeed 2, and six singles). A post-boot flip of a live process would have to change all 59 consistently; one missed reader is a paper strategy on a live order. **Fable's implementation under Trey's ruling (marked as Fable's):** one mode per process lifetime. The loader resolves mode = paper at boot unconditionally; `launchProfiles.production.mode` cannot select live at boot; `verifyTradingMode()` (`run-empire-v2.js:1486-1500`, the two-key env check) is deleted. "Live" is a deliberate relaunch: an operator command that carries a one-shot confirmation for that launch only (never written to `.env`, settings, or the pm2 file), logged with its own receipt (`paper → live, at, by`). No config, stamp, or flag can produce live on its own.

**R5. Singleton lock — RULED Sept 9: stays** ("absolutely needs to stay on and prevent a second copy that would be catastrophic"). The monitor exit (`SingletonLock.js:173-185`) and acquisition refusal (`:37-77`) are unchanged. Standing rule for every agent on the box: nothing but the running bot touches `.ogz-prime-v14.lock`. The rename-not-exclusive creation (`AtomicWrite.js:25`) is recorded as a known weakness for a later ruled change, not touched here.

**R6. Fault halts — RULED Sept 9: recovery with receipt and scream.** Enumerated Sept 9: authorized codes `StateManager.js:110-119` (8); callers: `OrderExecutor.js:103` (exit identity — "operator must reconcile active trade identity"), `:153` (`broker_order_reconciliation_required`), `:1195` (`exit_intent_reconciliation_required`), `:1461` (`exit_rail_broker_desync`), `run-empire-v2.js:2558, :2639` (exit-monitor reconciliation / direction integrity), `TtpCutoffEnforcer.js:813` (cutoff flatness — already resets at `:877-884`). Recovery conditions: the five reconciliation-class halts clear when a reconciliation pass for that symbol succeeds (local state = broker state, verified by REST), calling `resetSymbolHalt()` (`:4162`), writing the receipt (`symbol, code, condition met, at`), and buzzing at max priority. `BROKER_UNVERIFIABLE` clears when the broker becomes verifiable for that symbol. `DIRECTION_INTEGRITY_EXIT_REFUSAL` and `symbol_cooldown`: the former's condition is proposed in the packet for Trey; the latter is deleted (item 3). Halts-until-restart survive only where Trey rules so per code.

**R7. Ruled sizing values (A15; C-14) — RULED Sept 9: put them in.** 5% normal / 7.5% boosted / 25% total out enter `settings.json` at this boundary as a ruled value change in the exception ledger (today: 1% / 5% / 15% via pm2 stamps). Which leaf is which per the §1.2 map: `positionSizing.basePositionSize`=0.05, boosted → the confidence-scaled ceiling `positionSizing.maxPositionSize`=0.075, `entryLogic.sizing.absoluteCapPercent`=0.25. Executor confirms the multiplier stack (§slice 4: confidence ×2.5, confluence ×2) still cannot exceed 7.5% on one trade, or reports it.

**R8. Settings activation — RULED Sept 9 (A23): hot.** A saved setting applies to new decisions immediately. Globals never move an open position's parameters (frozen at entry, A21). **Per-trade edit contract (A23):** the UI may change one specific open trade's stop — that trade only, that moment only, never the global value — through the same single writer, producing its own receipt (`trade id, field, from, to, at, by`) and updating that trade's frozen policy alone. Two mechanisms, both on receipts. (R3's command-line confirmation flag is likewise Fable's proposal, marked as such.)

**R4. Exit freeze scope — RULED Sept 9: now.** Read Sept 9: `PolicyBuilder.buildForTrade` (`:557-598`) freezes `contract`, `profitManagement.{beScaleOut, breakEvenStop, tieredExit}`, `fees`, and the MTF snapshot; **it does not freeze trail config.** `ExitContractManager` already holds `trade.frozenExitPolicy` (`:871, :917`) but its post-entry reads go to globals: trail config captured at construction (`:263`, used `:729-735`), break-even from `BASE_CONFIG` (`:821` — the frozen copy exists in the policy and is ignored), fee buffer from `BASE_CONFIG.exitLogic.trail` (`:836`). The contract/timeframe lookup at `:936-948` happens at contract creation = entry time and stays. The change: add `exitLogic.trail` to `PolicyBuilder.CONFIG_PATHS` and freeze it; point `:729-735`, `:821`, `:836` at `trade.frozenExitPolicy`. Proof: change the global trail setting while a paper position is open; its trailing behavior is unchanged; a position opened after uses the new value. A21 becomes true, not PARTIAL.

---

## PART 0 — SEQUENCE AND PER-COMMIT ACCEPTANCE (B01, B18)

Commits land in this order, one behavior each. Each commit's acceptance is defined against the state it creates, never against a later commit's capability.

| # | Commit | Acceptance after this commit (box, paper, fresh boot) |
|---|---|---|
| 0 | Manifest 0.1c (docs) | cold-pull PASS both readers |
| 1 | Required-module list reconciled (B19; C-12): the executor **enumerates the required list by name** in the packet (every module the constructor and `start()` hard-require), removes `OptimizedTradingBrain` (`ModuleAutoLoader.js:253`), makes `loadDirectory()`'s catch (`:130-135, :245-250`) propagate a required-module failure instead of returning `{}`, and wires `validateModules()` (`:267-283`) so a missing required name refuses boot naming it | boot proceeds; the enumerated list and `validateModules` receipt match |
| 2 | Fatal handlers (B02, item 1; C-10): **two phases, one owner each.** *Boot phase* (module load through the end of `start()`): an uncaught failure is a boot refusal — exit non-zero, name the error; nothing is trading yet. *Runtime phase* (after `start()` resolves): uncaught exception / unhandled rejection → loud trace + max buzz + per-symbol quarantine **only when a symbol is known** (the enumeration of unknown-symbol paths is in the packet); process continues. The runtime listeners are registered in `main()` **before** `new OGZPrimeV14Bot()` (today the bot is constructed at `:3623` before the listeners at `:3628-3645`), and the bootstrap handlers (`:316-328`) plus SingletonLock's fatal handlers (`SingletonLock.js:157-166`) are removed. SIGINT/SIGTERM cleanup (`:144-154`) stays. Lock-monitor exits → R5. Receipt: after startup, the registered-listener list (one per event) and the boot-phase/runtime-phase boundary named by line. | rejection fired after start → alive, trace + buzz; exception during boot → exit ≠ 0, named |
| 3 | Startup exit code (B18, item 8; C-11): `shutdown()` is shared — SIGINT/SIGTERM call it (`run-empire-v2.js:3628-3629`) and must keep exiting 0. Add an exit-code parameter: `start()`'s catch calls `shutdown(1)`; operator signals call `shutdown()` = 0. Hold (`:1800-1840`) and TrAI-degraded (`:1841-1849`) unchanged. | forced start failure → exit ≠ 0; SIGTERM → exit 0; hold → alive + named |
| 4 | Migration commit (Part 1) | boot; config parity receipt (1.5); `.env` refusal test with a **legacy behavioral name** (B11) |
| 5–13 | Remaining ruled changes (Part 2), one each — items 2,3,4,6,9,10,11,12,13 = commits 5–13 | boot after each; the item's own receipt |
| 14 | **Switching (R2 ruled: build now):** schedule contract consumed end-to-end, 15-minute flatten, per-venue fee selection; paper profile → switching | boot; 30/15/5 fixture; one real RTH boundary on paper |
| 15 | Revisioned settings contract (1.7, R8 hot) | boot; receipt shows `settingsRevision` + hash |

A commit that fails its own acceptance gets no successor until fixed. No commit is tested against a capability introduced later.

---

## PART 1 — THE CUT (RULED Sept 4–8)

### 1.1 Files after the cut
- `config/settings.json` — every value a customer or Trey changes without code, including `PROFILE` (RULED Sept 6). The UI edits this file.
- `config/internals.json` — static system values.
- `.env` — credentials only. **Credential surface (B10):** `KRAKEN_API_KEY`, `KRAKEN_API_SECRET`, `ALPACA_API_KEY`, `ALPACA_API_SECRET`, `SENTRY_DSN`, `SIGNALSTACK_WEBHOOK_URL`, `WEBSOCKET_AUTH_TOKEN`, `NTFY_TOPIC`, `INCEPTION_API_KEY` (TrAI's LLM key — `trai.llm.apiKeyEnv`, `trai_llm_config.js:54-82`; D2 E0189), `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `DISCORD_STATS_WEBHOOK_URL`, `DISCORD_STATUS_WEBHOOK_URL`. Bootstrap inputs (`NODE_ENV`, `DOTENV_CONFIG_PATH`) are classified BOOTSTRAP, not credentials and not behavior; the pm2 file may carry them (D1 E0041, D2 E0111).
- **`.env` loaders today: four, not three (C-07).** ConfigLoader (`:26`), the two notifier modules (`telegramNotifier.js:44`, `discordNotifier.js:48`), and **`ecosystem.config.js:7`**, which loads `.env` before pm2 builds the bot's environment. After the cut: the runtime process has exactly one loader (ConfigLoader); the notifier loads are removed; the launcher's load in `ecosystem.config.js` is kept **solely** to deliver credential pass-throughs to pm2 (or replaced by pm2's `env_file`), documented as the launcher's load, and the receipt shows pm2-launched processes receive credentials.
- Deleted as behavioral sources: `config/features.json`, `config/trading.config.json` (once every leaf has moved), `profiles/*.env`, the pm2 env block's behavioral stamps.

### 1.2 Placement — leaf level, not block level (B07, B09)
Placement is by manifest row. **No whole-block choices.** Sizing map, verified (C-05 corrected v2's reversed lines): `positionSizing.basePositionSize` → readers `TradingLoop.js:2303`, `ModuleInitializer.js:51` via alias `sizing.basePositionSize` ← env `BASE_POSITION_SIZE` (`ConfigLoader.js:787, 1592, 2019`); `positionSizing.maxPositionSize` → readers `OrderExecutor.js:3107, 3118, 3919, 4187`, `PositionSizer.js:28` via alias ← `MAX_POSITION_SIZE_PCT` (`:788, 1593, 2020`) — the confidence-scaled base is read and scaled at `OrderExecutor.js:3104-3121`; `positionSizing.maxPositions` → `TradingLoop.js:1451, 1499, 1527` ← `MAX_POSITIONS` (`:789, 1594`); `entryLogic.sizing.absoluteCapPercent` → **`OrderExecutor.js:2063`** (the post-scaling cap) ← `ABSOLUTE_POSITION_CAP` (`:2023`). Four leaves, one owner each after the cut. `entryLogic.sizing.basePositionSize` and `.maxPositionSize`: no reader by path → DELETE with that receipt. Proven duplicates collapse only with producer/consumer/unit/destination recorded; value conflicts escalate to Trey.
Bundled rows (M0001, M0044, L0039 and any row describing more than one value) are split in 0.1c; nothing moves as a bundle.
Every `HOLD-NEEDS-OWNER` row is either ruled by Trey before the cut or listed in the packet as RETAINED-IN-PLACE — **and RETAINED-IN-PLACE is only legal for a code literal that has exactly one surviving owner compatible with the two-config architecture (C-06).** A HOLD on a deleted source, a fallback, or a second authority for a leaf that is moving **blocks the cut**; it cannot be retained. The cut claims no completeness while any such HOLD exists.

### 1.3 The loader after the cut
- One `load()` at boot; `get()` everywhere else. The runtime re-loads (`PatternMemoryBank.js:161`, `UnifiedPatternMemory.js:184`) exist because singleton callers construct pattern memory with no config (`trai_core.js:123` → `getUnifiedPatternMemory()`; `UnifiedPatternMemory.js:1194`). **Order (C-08):** first inject resolved mode, asset bucket, persistence flag, and data path through every singleton caller; then remove the reload and direct-env fallbacks. Not the reverse.
- `PROFILE` from `settings.json`; mode derived from it; the eight mode stamps deleted as inputs; their direct readers (`SingletonLock.js:26-28`, `BotStateFrame.js`, `instrument.js:57`, `run-empire-v2.js:1772-1778`) read the derived values.
- Every direct `process.env` read on the runtime path rewired to the loader. Denominator (B10): Amp's census **plus** `ogz-meta/claudito-logger.js:327-403` (publisher: `OGZ_ACCOUNT_*`, `OGZ_MIN_TRADES_REQUIRED`, `OGZ_TRACK_RECORD_START_AT`, `TTP_MAX_LOSS_THRESHOLD_EQUITY`, `TTP_PROFIT_TARGET_DOLLARS`, `OGZ_MAX_DRAWDOWN`, `OGZ_PROFIT_TARGET`, `TTP_MAX_LOSS_DOLLARS`, `STARTING_BALANCE`, `BROKER`), `TRAIDecisionModule.js:90` (`TRAI_PATTERN_PACK_PATH`), the two notifier modules, `NtfyTraceNotifier.js:194-201`. Each is a manifest row with a destination.
- **Refusal (B11):** the loader carries an explicit map `legacyEnvName → canonical settings path` (e.g. `STOP_LOSS_PERCENT → exits.stopLossPercent`, built from `ConfigLoader.js:853-880` and the full env census). Any legacy behavioral name present in process env or `.env` → refuse boot, name the legacy name and its canonical path. Bootstrap and credential names are exempt by list. The refusal receipt exercises a real legacy name, not a synthetic key.
- **One owner, zero defaults (Sept 8):** no defaults or fallback tables in the loader or on the trade path. Downstream constructor/parameter fallbacks are discharged, not grandfathered (B12): e.g. `FairValueGapDetector.js:25-27` `config.minFVGPercent || 0.05` turns a permitted zero into 0.05 — consumers bind to the canonical setting with explicit missing/invalid handling; valid zero preserved where the schema allows it. KEEP-AS-CODE-CONSTANT requires a stated reason other than "the parent normally supplies it."
- Source tracking; strict parsing (Sol S3); one read surface — `BASE_CONFIG` / `getAll()` / `getExitContract()` / `getTimeframeConfig()` return the snapshot or are deleted (S6); snapshot completeness (S2); warnings surfaced (S7).
- `ecosystem.config.js` env block for `ogz-prime-v2`: `NODE_ENV`, `DOTENV_CONFIG_PATH` (bootstrap), credential pass-throughs only.

### 1.4 New settings (RULED Sept 6)
- `lossWarnings.lossesInARow: [3,4,5]`, `lossWarnings.drawdownPercent: [5,10,15]` — warnings only; implementation is stop 8; the numbers exist now.
- `dailyLossTimeoutPercent: 50` — halt new entries only, nothing sold, user restarts; implementation is stop 8.
- Publisher block per R1.

### 1.5 No behavior change — the proof (B06)
Parity is over **consumer-effective values**, not the loader snapshot. For every row: the old source and the value each consumer actually received at `e54a8b8` (via `get()`, compat mapping, `BASE_CONFIG`, active overrides, `getExitContract()`), the new canonical path, and the value received after the cut. Equal, or listed in the exception ledger with its ruling. Snapshot-shape repairs (S2 fields that were `undefined`) are recorded separately from value changes.
Baseline: the box's actual process environment for the running paper profile, captured privately by the executor on the box (never in the receipts), not the frozen pm2 file plus five remembered `.env` values. Secrets never appear in any receipt.
Structural parity (B08): empty arrays, empty objects, null, false, zero, empty string are distinct cases and must survive the migration; required containers (e.g. `launchProfiles.paper.strategies.soloFilter: []`, required by `ConfigLoader.js:149-163, 204-213`) are present in the new files. The completeness receipt proves structure, not only leaf paths.

### 1.6 Manifest 0.1c (before the cut)
- D1 rows: E0041 → BOOTSTRAP (not `.env`); E0114 `PROFILE` → SETTINGS (conflict with v1 resolved); M0042 and M0043 → RULING-NEEDED (decision-affecting cadence and level truncation in `BreakAndRetest.js:133-150, :266-285`, not performance plumbing); M0081 and M0082 → split per parameter, `||` fallbacks named, KEEP withdrawn; Q0132/Q0133 → DELETE-RULED with the CIRCUIT_BREAKER block.
- D2: E0111 note corrected (BOOTSTRAP, pm2 permitted); E0189 `INCEPTION_API_KEY` → credential surface with the TrAI reader cited; E0190 `STARTING_BALANCE` → live publisher input, owner map required.
- Add rows: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `DISCORD_STATS_WEBHOOK_URL`, `DISCORD_STATUS_WEBHOOK_URL`, `TRAI_PATTERN_PACK_PATH`, `OGZ_MAX_DRAWDOWN`, `OGZ_PROFIT_TARGET`, `TTP_MAX_LOSS_DOLLARS`, publisher's `STARTING_BALANCE`/`BROKER` reads, and every required empty container as a structural row.
- Split every bundled row into one row per value.
- Add every `|| <number>` config-fed fallback from Part 5 (179) as DECISION-/CONSTRUCTOR-LITERAL rows with file:line and the value it would override.
- Cold-pulled by both readers. Then the cut.

### 1.7 Revisioned settings (RULED Sept 8, A21; B13, B14; scope per R4)
- **One atomic settings writer** (C-15): a single write path used by the UI and every other caller increments `revision`, records `updatedAt`, computes a content hash over every nested value (replaces the broken fingerprint, S1). Direct file writes that bypass it are rejected at load (hash mismatch → refuse boot, name it). Activation (next boot vs hot) → **R8**.
- Every signal, order, and trade receipt records `settingsRevision` + `settingsHash`.
- Exit consumers per R4: (a) rewired to the frozen policy now, or (b) listed by file:line as reading global config after entry, with A21 recorded PARTIAL until stop 6.

---

## PART 2 — RULED CHANGES AT THIS BOUNDARY (one commit each, in the Part 0 order)

1. **Fatal handlers** — as Part 0 #2. (B02, B17: global handlers cover uncaught failures only; producer-caught failures — publisher `claudito-logger.js:717-729`, notifier delivery `NtfyTraceNotifier.js:175-201` — are inventoried and classified separately; a caught failure that only logs is a named absence on the stop-2/7 sheets, not claimed fixed here.)
2. **Live-requires-eval weld** (RULED Sept 6): delete `ConfigLoader.js:1250-1254`. Receipt shows only that condition changed.
3. **Per-symbol loss cooldown** (RULED Sept 6): setting, stamp, and reader `StateManager.js:1676, :3973-3979` deleted, eval included.
4. **Shorts gate** (RULED Sept 3): paper `pipeline.directionFilter = "both"`. Packet states: five strategies carry `allowShorts=false`; eight strategies' short paths unverified; one-trade-per-asset unverified. Gate only.
5. **Switching** — per R2. If (b): the paper profile stays `static/stocks`; item 5 becomes a stop-8 item with B03/B04/B05 as its opening facts.
6. **Named absence for the phone**: `run-empire-v2.js:1071-1077` — missing `NTFY_TOPIC` → printed + `NTFY_NOTIFIER_ABSENT` trace. Absence is not delivery; delivery receipts distinguish configured / absent / attempted / failed / delivered (B17).
7. **Autoloader honesty** — as Part 0 #1 (B19).
8. **Startup failure exit code** — as Part 0 #3 (B18).
9. **Route-specific credentials** (S9): `validateEnvironment()` requires only the brokers the active route uses. With R2(b), paper requires Alpaca only.
10. **One tier identity** (S12): `TRADING_TIER` direct read removed; one tier value in settings; one scaling table.
11. **Circuit breaker** (RULED Sept 7, Fourth Shape): `features.json` CIRCUIT_BREAKER block and `core/ErrorHandler.js` deleted (no live reader — `ErrorHandler.js` zero references from the runtime). Producer side is item 1 for uncaught failures; caught failures per item 1's inventory.
13. **Fees per venue** (RULED Sept 8, Trey: "there's only one fee structure because Alpaca doesn't charge fees to trade"; resolves B05): the fee table in the JSON is Trade The Pool's per-share model; the pm2 stamp applies it globally, so the paper bot on Alpaca has been charging itself fees Alpaca does not charge (slice 5 finding, `FeeModel.js:32-58` reads one global `fees.*`). Settings carries one fee block per venue. **Commission ≠ slippage (C-04):** A22 rules commission; `fees.slippage` (0.0005) is simulated execution friction applied to paper/backtest fill prices (`OrderExecutor.js:3604-3614`, refuses if non-finite). So `fees.alpaca` = commission fields zero, **slippage preserved at today's consumer-effective 0.0005** unless Trey rules it separately; `fees.kraken` = the JSON's maker/taker/slippage; `fees.ttp` = the per-share eval model. **Ruled value change** in the exception ledger: Alpaca's commission goes from the TTP model to zero. Venue selection in `FeeModel.fromTradingConfig()` by active broker: under R2(b), the migration writes the Alpaca block and the single reader consumes it, selection code lands at stop 4; under R2(a), selection code lands in item 5's commit.
12. **Boot cosmetics** (Trey Sept 6): emoji lock lines, checkpoint counters, the "Desktop Claude + Browser Claude = MERGED" banner removed. **The lock name `ogz-prime-v14` is NOT cosmetic** (B20): it is the lock filename and coordination identity (`SingletonLock.js:10-18, :46-82`). Unchanged in this boundary; a rename is its own ruled change with migration semantics.

Not in this boundary (later stops, opening facts recorded): Alpaca heartbeat/silence watchdog + never-clearing fault record (stop 2); notifier keyword filter (stop 2); DPS wiring, sizing formula, PID wiring (stop 4); per-venue fee selection code when a second broker is active (stop 4/8); four exit layers → one, exit consumers → frozen policy if R4(b) (stop 6); publisher's caught failure (stop 7); SmartMoneySweep internal loss gate (stop 3); TrAI init failure (stop 10); switching if R2(b) (stop 8).

---

## PART 3 — THE BRIDGE COMMITS (B21, corrected record)
Six commits (`66d852fc`, `3b2b7aef`, `5640f7fd`, `170ca89c`, `c51e76c2`, `b6bbd44c`). Four touch bridge/config/test/tooling files only. **Two touch `core/persistent_llm_client.js`**, which TrAI uses at runtime (`trai_core.js:55, :126-134`): `5640f7fd` raised the client's permitted timeout ceiling 300,000 → 600,000 ms (TrAI's own configured timeout is still capped at 300,000 by `trai_llm_config.js:54-82`, so TrAI's timeout did not change); `c51e76c2` added usage/stop-reason metadata handling. Neither changes strategy math. Both are recorded as runtime-path changes, with their tests, not as "touching nothing the bot runs." v1's statement to that effect is withdrawn.

---

## PART 5 — ADJACENT-CLASS SWEEP (Trey, Sept 8: "check for the adjacent bug class types"; run across run-empire-v2.js, core/, brokers/, modules/, foundation/, utils/, ogz-meta/claudito-logger.js, instrument.js at e54a8b8)

| Class (found once) | Swept | Result |
|---|---|---|
| `process.exit` sites | 15 | run-empire `:320/:328` (bootstrap handlers → item 1), `:598` (constructor refusal, legit), `:3586` (shutdown exit 0 → item 8), `:3655` (main catch, legit); SingletonLock `:77/:115` (lock-acquire refusals, legit), `:147` (SIGINT/SIGTERM exit 0 = operator kill, keep), `:160/:166` (fatal handlers → item 1), `:179/:185` (lock monitor → R5), `:327` (comment); BacktestRunner `:547/:552` (backtest only). |
| fatal-event registrations | 6 | `uncaughtException`/`unhandledRejection` registered three times each (run-empire bootstrap, run-empire main, SingletonLock). Item 1 collapses to one. |
| independent `.env` loads | 3 | ConfigLoader (the one), `utils/telegramNotifier.js:44`, `utils/discordNotifier.js:48`. B10 complete. |
| file watchers | 0 fs.watch; 1 poll | the lock monitor (`SingletonLock.js:175`, setInterval) — R5. |
| phantom module names | 1 | `OptimizedTradingBrain` (required, absent) — item 7. `ExecutionRateLimiter` is commented out (`run-empire-v2.js:451-452`). `utils/tradeLogger.js` is a 3-line shim over `core/tradeLogger.js`, not a fork. |
| `\|\| <number>` fallbacks on config-fed values (the FVG zero-eater) | **179** (script total at e54a8b8; the per-file list below shows the top 15 files = 155; the remaining 24 are in files not listed — the full 179-row table is `scripts/sweep-classes.sh` class-6 output attached to 0.1c, C-14b) | IndicatorEngine 34, SmartMoneySweep 33, BreakAndRetest 12, PatternBasedExitModel 11, LiquiditySweepDetector 8, PIDController 8, DynamicTrailingStop 7 (dead module), VolumeProfile 7, MAExtensionFilter 7, TwoPoleOscillator 6+4, RegimeDetector 6, TradeJournal 4, PerformanceValidator 4, EventLoopMonitor 4. Each is a second authority that overrides a legitimate 0/false. **0.1c enumerates every one as a DECISION-/CONSTRUCTOR-LITERAL row**; rule 1.3 applies (bind to canonical, preserve valid zero). |
| validators/checkers with zero direct callers | 9 | `checkInvalidation`, `checkSourceCompleteness`, `checkVetoConditions`, `validateExitContext`, `validateExitContract`, `validateIndicators`, `validateQuantity`, `validateSignal`, `validateTrade`. Dead safety code that reads as protection. Each wired or deleted at its stop on Trey's word; none touched in this boundary. (Dynamic dispatch could hide a caller; the executor confirms with references, not grep.) |
| watchdog timers that can block/flatten/exit | 3 | lock monitor (exit — R5); `ResilientWebSocket.js:516` data-silence watchdog (→ stale → fault, stop 2); liveness watchdog (run-empire `startLivenessWatchdog`, 182 hits in the box log; stop 2). |
| block records with no clearing path | 3 | `brokerTruthUnavailable` / `entryBlock` (OrderRouter/OrderExecutor, stop 2, known); **symbol halts** (`StateManager.haltSymbol`, no clear — **R6**). `pauseTrading` has 6 callers and every one has a resume path (SessionRouter transitions, WebSocketManager, TradeJournalBridge). |

## PART 4 — RECEIPTS REQUIRED BEFORE COLD-PULL
Packet with tapes; per-commit boot receipts per Part 0; consumer-effective parity table (1.5) with the exception ledger; structural completeness receipt (1.5); registered-listener list after boot (Part 0 #2); required-module list receipt (#1); refusal receipt with a legacy name (1.3); fingerprint/hash change proof; `process.env` reads remaining on the runtime path (must be zero outside the loader and the credential surface); `settingsRevision` on a receipt (1.7); existing tests green (not proof).

Both readers cold-pull. Disagreement → Trey. Trey says land. Then, **on a separate explicit activation word from Trey (C-19)**: `pm2 delete ogz-prime-v2` then `pm2 start ecosystem.config.js --only ogz-prime-v2` (never `pm2 restart` — pm2 reuses the cached environment and the deleted stamps come back), `NTFY_TOPIC` present, boot trace archived to `ogz-meta/inbox/trey/<date>/`.

---
**WHAT I DID (v3, Sept 9 full read):** `SessionRouter.js` 40-140, 275-545, 1003-1113, 1481-1576; `PolicyBuilder.js` 555-605; `ExitContractManager.js` 253-266, 724-740, 815-842, 871, 917, 930-950; all 7 `haltSymbol()` call sites + `StateManager.js:108-122`; `run-empire-v2.js` 1486-1500; mode-consumer census (59 reads / 17 files). Not read: `SessionRouter.js` 545-1003, 1113-1856 (stop 8). Then re-verified every code claim in Codex's Sept 9 attack at `e54a8b8` (`StateManager.js:144-147, 4036, 4162`; `TtpCutoffEnforcer.js:877-884`; `OrderExecutor.js:2063, 3104-3121, 3604-3614`; `ecosystem.config.js:7`; `run-empire-v2.js:3623-3645`; `trai_core.js:123`; `UnifiedPatternMemory.js:182-184`; `StateManager.js:117, 134, 1676, 3973-4011`; `claudito-logger.js:576-582, 717-729`; `AtomicWrite.js:25`); rewrote R1, R5, R6, added R7, R8, corrected B01/B05/B07/B09/B10/B12/B13/B18/B19 text. **WHAT I DID (v2):** read Sol's grand overview in full; verified every B-section code claim against `e54a8b8` myself (`SingletonLock.js:144-200`, `ModuleAutoLoader.js:136-283` and the absent `OptimizedTradingBrain.js`, `SessionRouter.js:46-60, 116-124, 279-293, 313, 341-351, 355-470`, `ConfigLoader.js:572-590, 665-683`, `run-empire-v2.js:388-392, 425-437, 863-876`, `FeeModel.js:32-58`, `OrderExecutor.js:2063-2074, 2374-2390`, `utils/telegramNotifier.js:44-48`, `utils/discordNotifier.js:48-54`, `TRAIDecisionModule.js:90`, `ExitContractManager.js:158, 253-263, 729, 821, 836, 948, 1017-1019`, `PolicyBuilder.js:561-616`); rewrote the spec from those reads.
**WHAT I DID NOT DO:** read `trai_core.js` and `trai_llm_config.js` myself (Part 3 relies on Sol's cited lines); read `BreakAndRetest.js` or `FairValueGapDetector.js` myself (1.6 relies on Sol's D1 receipts and fixture); boot anything; verify the box environment.
**WHAT I ASSUMED:** Sol's D1/D2 line citations for the files I did not open are accurate; R1–R4 are Trey's to answer and nothing here presumes the answer.
