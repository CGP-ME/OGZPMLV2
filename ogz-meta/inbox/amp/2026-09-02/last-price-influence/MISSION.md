# Mission

Dispatcher: Puck thread `T-01a034e9-036f-70af-8009-eb719c05fc40`.

Ruling authority: Trey.

## Part D

Name the SessionRouter broker bar's last-price influence in
`run-empire-v2.js` with exactly one trace emission while preserving the ruled
strategy boundary: every normalized frame may update the mark, but only the
active timeframe may reach strategy handling.

During review, the trusted-host probe proved the inherited SessionRouter
producer passed seconds into a StateManager map seeded by REST milliseconds.
Trey classified this as an internal producer defect and authorized Fourth Shape
minimum surgery: normalize that producer timestamp into canonical milliseconds
before `StateManager.updateLastPrice`, preserve the existing monotonic rejection,
and prove a genuinely later frame advances the mark while an older frame remains
rejected.

No new gate, flag, config, fallback, wrapper, downstream exception, broker/PM2
action, runtime activation, or adjacent behavior change is authorized. Part E
remains stopped until Part D is pushed and cold-pull cleared.
