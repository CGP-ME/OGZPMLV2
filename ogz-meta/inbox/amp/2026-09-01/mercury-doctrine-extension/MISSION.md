# Mission

Dispatcher: Puck thread `T-01a034e9-036f-70af-8009-eb719c05fc40`
Ruling authority: Trey
Mandate: HYGIENE
Executor: Amp on trusted VPS runner `ogzprime-prod-001`

## Tasking (verbatim)

> MISSION: MERCURY DOCTRINE EXTENSION. Mandate: HYGIENE. Trey's word.
> Territory: Mercury's prompt/rules-as-greps, review output schema, automatic pre-answer scan in `ask.js`, and `mercury.ignore` — all in `trai_brain/mercury-bridge/` + config. Bot code untouched. Ladder shape only: every new obligation is a capped verdict + named absence, never refusal to run.
>
> 1. TOTALITY → CANDIDATE SET. Prompt carries Totality law verbatim. Verdicts require `CANDIDATE SET`: every file, consumer, history entry examined, counted N of M, before verdict. Coverage below diff touched-file count caps UNVERIFIED with `coverage_insufficient` named.
> 2. AST-FIRST, EXTENDED TRIGGER. Automatic pre-answer scan fires on every changed `.js` file and every env-var/config-key name touched in diff (`find_references` on symbol), not `.js` only. AST-capable review citing zero AST evidence is capped with `ast_evidence_absent`.
> 3. INHERITANCE → WHOLE-FILE READ. Reviewing hunk means reading file. Required `INHERITED` section names inherited violations in every touched file (`|| 0` on trading data, swallowed catches, bypass env reads, silent defaults), unfixed.
> 4. FOURTH SHAPE CLASSIFIER. Every throw/gate/guard/fallback added in diff classified `producer-fixable` / `true boundary` / `unreachability-receipted`. Unclassified addition = named break, not pass.
> 5. SANDBOX AUTHORITY. Model-sandbox `run_check` has no authority for test/build claims. Tag execution provenance; verdict may cite only trusted-path results for pass/fail.
> 6. TESTIMONY VS RECEIPT. Docs/snapshots/ledgers/session forms/CHANGELOG are claims; code at HEAD is receipt. Prose-only allegation labeled `TESTIMONY`, cannot independently raise found_break. Add `ogz-meta/ledger/**` to `mercury.ignore` pending Trey's per-file rulings.
> 7. ALLEGATION CLASS. Every finding tagged `MECHANICAL` or `SUBSTANTIVE`. Executors may adjudicate MECHANICAL. SUBSTANTIVE non-convergence ends only `convergence` or `UNRESOLVED-FOR-TREY` with each seat quoted—never pending, never executor-adjudicated. Ruling 11 machine-side.
> 8. REPORT LAW. Output contains `WHAT I EXAMINED` / `DID NOT EXAMINE` / `ASSUMED`. Extend rules-as-greps banned list: “first time”; estimated percentages without numerator/denominator; announced workarounds; test counts offered as proof.
>
> Receipt requirement: review same artifact before and after; compare tool-call counts by type, Serena calls, candidate-set size, and required-section presence. Full layer reviews change itself; record objections. Accountability packet per ruling 7/7a under `ogz-meta/inbox/amp/<date>/mercury-doctrine-extension/`, redacted tapes + dual hashes. One atomic commit, one-shot `Amp <amp@ampcode.com>`, push only `codex/multi-asset-symbol-state`, HOLD for Fable cold-pull. No bot code, env/config keys/defaults/frameworks, PM2, broker, activation, main, destructive Git, or unrelated work.

Part C reconciliation, Part D, and Part E remain held and excluded.
