# Inherited violations

## Bridge files

- `ask.js` retains inherited broad operational responsibilities and existing environment/config loading. No new env/config key/default was introduced.
- `run-ledger.js` retains legacy no-panel structured review paths for backward compatibility. Prose inference was removed; panel receipts now bypass those paths entirely.
- `adversarial-review.js` retains prompt-only provider limitations; model-sandbox execution remains testimony and cannot establish host test/build truth.
- The doctrine scan named incomplete whole-file/inherited/AST coverage. Those absences remain unfixed and cap review authority.

## Tests and docs

- Test files retain large historical fixture surfaces. This mission changed only focused reviewer-panel/ledger/packet behavior tests.
- The moved ruling file preserves older undated-at-top content under its original 2026-08-29 title and appends a dated 2026-09-01 section; no earlier ruling text was rewritten.
- The historical Part A `MISSION.md` path reference was updated only because Trey explicitly ordered exactly two packet cross-reference updates. Its remaining historical task text is unchanged.

No trading data defaults, swallowed execution-path catches, broker bypass, PM2 behavior, or bot runtime code was introduced or modified.
