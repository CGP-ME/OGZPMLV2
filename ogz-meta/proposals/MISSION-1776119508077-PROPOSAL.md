# PROPOSAL: MISSION-1776119508077
Generated: 2026-04-13T22:32:39.371Z

## ⚠️ ADVISORY MODE - NO CHANGES MADE
This document proposes changes for human review.
**Nothing has been modified. You must approve before execution.**

---

## Issue
audit ogz-meta/test-fixtures/c2c-race-condition.js for correctness issues — focus on concurrency and async patterns

## RAG Context Retrieved
- [CRITICAL] FIX-2026-03-26-LONG-ONLY-PIPELINE: Zero shorts firing in backtest despite SmartMoneySweep generating 530 short signals. PnL calculation...
- [HIGH] AUDIT-2026-04-07-ENV-VAR-THEATER: Sweep presets producing duplicate results - 3 of 5 configs returned identical $54.07...
- [HIGH] BUG-2026-03-04-DUPLICATE-PATTERN-FILES: Pattern bank shows 8181 patterns when we deleted them. Two different pattern files exist....

## Bugs Identified

### Bug 1: race_condition_toctou
- **Location**: ogz-meta/test-fixtures/c2c-race-condition.js:26
- **Description**: TOCTOU race condition: `trade.remainingSize` is read before an async `await broker.sendCloseOrder` call, and then updated afterwards using the stale value, which can cause negative sizes or double‑counting if another concurrent caller modifies the trade in the meantime.
- **Fix Type**: race_condition_toctou
- **Score**: N/A


### Bug 2: async_anti_pattern_forEach
- **Location**: ogz-meta/test-fixtures/c2c-race-condition.js:53
- **Description**: `processOrdersBatch` uses `Array.forEach` with an async callback, so the function returns before any of the async `validateAndExecute` calls complete, resulting in an always‑empty `results` array.
- **Fix Type**: async_anti_pattern_forEach
- **Score**: N/A


## Proposed Fixes

### Proposal 1: 0
- **Location**: ogz-meta/test-fixtures/c2c-race-condition.js:25
- **Proposed Change**:   // Compute the size to close based on the most recent remainingSize
  const closeSize = trade.remainingSize * fraction;
  // Send the close order
  await broker.sendCloseOrder(trade.symbol, closeSize);
  // Safely update remainingSize, guarding against race‑condition updates
  // and preventing negative values
  trade.remainingSize = Math.max(trade.remainingSize - closeSize, 0);
- **Status**: READY_TO_APPLY

#### Replacement (ready to apply)
```javascript
// BEFORE:   // RACE WINDOW START: trade.remainingSize is read here
  const closeSize = trade.remainingSize * fraction;

  // Async gap: broker call takes network time
  // Another caller could mutate trade.remainingSize during this await
  await broker.sendCloseOrder(trade.symbol, closeSize);

  // RACE WINDOW END: updating with stale reference
  // If another caller already reduced remainingSize, this calculation
  // uses the old value, potentially going negative or double-counting
  trade.remainingSize -= closeSize;
// AFTER:    // Compute the size to close based on the most recent remainingSize
  const closeSize = trade.remainingSize * fraction;
  // Send the close order
  await broker.sendCloseOrder(trade.symbol, closeSize);
  // Safely update remainingSize, guarding against race‑condition updates
  // and preventing negative values
  trade.remainingSize = Math.max(trade.remainingSize - closeSize, 0);
```


### Proposal 2: 1
- **Location**: ogz-meta/test-fixtures/c2c-race-condition.js:48
- **Proposed Change**: async function processOrdersBatch(orders) {
  // Execute all orders in parallel and wait for all promises to settle
  const results = await Promise.all(orders.map(order => validateAndExecute(order)));
  return results;
}
- **Status**: READY_TO_APPLY

#### Replacement (ready to apply)
```javascript
// BEFORE: async function processOrdersBatch(orders) {
  const results = [];

  // Bug: forEach does NOT await async callbacks
  // This returns immediately with an empty results array
  orders.forEach(async (order) => {
    const result = await validateAndExecute(order);
    results.push(result);
  });

  return results;  // Always empty — promises haven't resolved yet
}
// AFTER:  async function processOrdersBatch(orders) {
  // Execute all orders in parallel and wait for all promises to settle
  const results = await Promise.all(orders.map(order => validateAndExecute(order)));
  return results;
}
```


## Impact Analysis
- Files potentially affected: run-empire-v2.js (main), core/indicators/IndicatorEngine.js, brokers/BrokerFactory.js, core/StateManager.js
- Dependencies: Empire V2 Architecture, IBrokerAdapter

## To Approve
Run: `node ogz-meta/approve.js MISSION-1776119508077`

## To Reject
Run: `node ogz-meta/reject.js MISSION-1776119508077`

---
Generated by Claudito Pipeline (Advisory Mode)
