# OGZPrime Claude Bootstrap

This file is a bootstrap only. The canonical doctrine is:

`ogz-meta/Alignment/TheDoctrine.md`

Read The Doctrine before non-trivial work. It is load-bearing, not advisory.
Hookify exists to enforce The Doctrine.

If this file, `CLAUDE.md`, any AGENTS file, hook, memory, command snippet, or
session note conflicts with The Doctrine, stop and report the contradiction.
The Doctrine wins for agent behavior unless the current user instruction
explicitly supersedes it.

Do not make production code changes without explicit approval. Verify the live
repo, current source, current command/help output, and relevant runtime/log state
before making claims.
